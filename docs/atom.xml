<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Threcial</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://threcial.github.io/</id>
  <link href="https://threcial.github.io/" rel="alternate"/>
  <link href="https://threcial.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, Threcial</rights>
  <subtitle>Threcial 的技术笔记？</subtitle>
  <title>Threcial</title>
  <updated>2026-07-03T07:51:56.000Z</updated>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="linux" scheme="https://threcial.github.io/tags/linux/"/>
    <category term="nginx" scheme="https://threcial.github.io/tags/nginx/"/>
    <category term="wordpress" scheme="https://threcial.github.io/tags/wordpress/"/>
    <category term="优化" scheme="https://threcial.github.io/tags/%E4%BC%98%E5%8C%96/"/>
    <content>
      <![CDATA[<p>服务器环境：Linux、Nginx、PHP、WordPress、Sakurairo 主题<br>服务器类型：轻量应用服务器<br>优化目标：提高网站访问速度、降低资源占用、增强基础安全防护、减少异常访问对服务器的影响。</p><hr><h2 id="1-Linux-系统层优化"><a href="#1-Linux-系统层优化" class="headerlink" title="1. Linux 系统层优化"></a>1. Linux 系统层优化</h2><h3 id="1-1-增加-Swap-交换空间"><a href="#1-1-增加-Swap-交换空间" class="headerlink" title="1.1 增加 Swap 交换空间"></a>1.1 增加 Swap 交换空间</h3><p>服务器为轻量应用服务器，默认未配置 Swap。在内存较小的情况下，PHP、MySQL、Nginx 或 WordPress 后台任务可能会导致内存瞬间升高，严重时触发 OOM，导致服务异常退出。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">free -h</span><br><span class="line"><span class="built_in">dd</span> <span class="keyword">if</span>=/dev/zero of=/swapfile bs=1M count=2048</span><br><span class="line"><span class="built_in">chmod</span> 600 /swapfile</span><br><span class="line">mkswap /swapfile</span><br><span class="line">swapon /swapfile</span><br></pre></td></tr></table></figure><p>设置开机自动挂载</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;/swapfile none swap sw 0 0&#x27;</span> &gt;&gt; /etc/fstab</span><br></pre></td></tr></table></figure><p>调整 Swap 使用优先级</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;vm.swappiness = 60&#x27;</span> &gt;&gt; /etc/sysctl.conf</span><br><span class="line">sysctl -p</span><br></pre></td></tr></table></figure><p>验证结果</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">free -h</span><br><span class="line">swapon --show</span><br><span class="line"><span class="built_in">cat</span> /proc/sys/vm/swappiness</span><br></pre></td></tr></table></figure><hr><h3 id="1-2-开启防火墙，仅放行必要端口"><a href="#1-2-开启防火墙，仅放行必要端口" class="headerlink" title="1.2 开启防火墙，仅放行必要端口"></a>1.2 开启防火墙，仅放行必要端口</h3><p>为了减少暴露面，只开放网站运行和远程维护所需端口，例如 HTTP、HTTPS 和自定义 SSH 端口</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">systemctl <span class="built_in">enable</span> firewalld --now</span><br><span class="line">firewall-cmd --state</span><br></pre></td></tr></table></figure><p>放行必要端口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">firewall-cmd --permanent --add-service=http</span><br><span class="line">firewall-cmd --permanent --add-service=https</span><br><span class="line">firewall-cmd --permanent --add-port=端口/tcp</span><br><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure><p>查看当前规则：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">firewall-cmd --list-all</span><br></pre></td></tr></table></figure><hr><h3 id="1-3-修改-SSH-默认端口"><a href="#1-3-修改-SSH-默认端口" class="headerlink" title="1.3 修改 SSH 默认端口"></a>1.3 修改 SSH 默认端口</h3><p>将 SSH 从默认 22 端口修改为自定义端口，减少互联网上针对默认端口的自动化扫描和暴力破解请求。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/ssh/sshd_config</span><br></pre></td></tr></table></figure><p>修改或添加：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Port 12345</span><br></pre></td></tr></table></figure><p>防火墙放行新端口后，重启 SSH 服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">firewall-cmd --permanent --add-port=12345/tcp</span><br><span class="line">firewall-cmd --reload</span><br><span class="line">systemctl restart sshd</span><br></pre></td></tr></table></figure><p>验证监听端口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ss -lntp | grep sshd</span><br></pre></td></tr></table></figure><p>确认新端口可连接后，再移除旧端口放行规则：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">firewall-cmd --permanent --remove-service=ssh</span><br><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure><hr><h3 id="1-4-SSH-配置密钥登录，关闭密码登录和空密码登录"><a href="#1-4-SSH-配置密钥登录，关闭密码登录和空密码登录" class="headerlink" title="1.4 SSH 配置密钥登录，关闭密码登录和空密码登录"></a>1.4 SSH 配置密钥登录，关闭密码登录和空密码登录</h3><p>为了增强远程登录安全性，使用 SSH 密钥登录，关闭密码登录和空密码登录。</p><p>在客户端生成密钥：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen -t ed25519 -C <span class="string">&quot;server-login&quot;</span></span><br></pre></td></tr></table></figure><p>将公钥上传到服务器：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-copy-id -p 12340 root@服务器IP</span><br></pre></td></tr></table></figure><p>修改 SSH 配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/ssh/sshd_config</span><br></pre></td></tr></table></figure><p>配置内容：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">PubkeyAuthentication yes</span><br><span class="line">PasswordAuthentication no</span><br><span class="line">PermitEmptyPasswords no</span><br><span class="line">PermitRootLogin prohibit-password</span><br></pre></td></tr></table></figure><p>检查配置语法：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sshd -t</span><br></pre></td></tr></table></figure><p>重启 SSH：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl restart sshd</span><br></pre></td></tr></table></figure><hr><h3 id="1-5-限制-SSH-登录尝试次数和连接数"><a href="#1-5-限制-SSH-登录尝试次数和连接数" class="headerlink" title="1.5 限制 SSH 登录尝试次数和连接数"></a>1.5 限制 SSH 登录尝试次数和连接数</h3><p>为了防止攻击者大量并发连接 SSH，占用系统资源，对 SSH 登录时间、认证次数和并发连接数进行限制。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/ssh/sshd_config</span><br></pre></td></tr></table></figure><p>添加</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">raceTime 20：客户端连接后必须在 20 秒内完成认证。</span><br><span class="line">MaxAuthTries 3：每个连接最多尝试 3 次认证。</span><br><span class="line">MaxSessions 2：限制单个 SSH 连接可打开的会话数量。</span><br><span class="line">MaxStartups 3:30:10：限制未认证连接数量，超过后按比例拒绝，最多允许 10 个。</span><br><span class="line">ClientAliveInterval 300：每 300 秒检测一次客户端是否在线。</span><br><span class="line">ClientAliveCountMax 2：连续 2 次无响应后断开连接。</span><br></pre></td></tr></table></figure><p>检查并重启：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sshd -t</span><br><span class="line">systemctl restart sshd</span><br></pre></td></tr></table></figure><hr><h3 id="1-6-安装-Fail2ban，自动封禁异常-IP"><a href="#1-6-安装-Fail2ban，自动封禁异常-IP" class="headerlink" title="1.6 安装 Fail2ban，自动封禁异常 IP"></a>1.6 安装 Fail2ban，自动封禁异常 IP</h3><p>为了自动处理 SSH 暴力破解、Nginx 异常访问、WordPress 扫描等行为，安装 Fail2ban 并配置规则。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">dnf install epel-release -y</span><br><span class="line">dnf install fail2ban -y</span><br><span class="line">systemctl <span class="built_in">enable</span> fail2ban --now</span><br></pre></td></tr></table></figure><p>创建本地配置文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/fail2ban/jail.local</span><br></pre></td></tr></table></figure><p>示例配置：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[sshd]</span></span><br><span class="line"><span class="attr">enabled</span> = <span class="literal">true</span></span><br><span class="line"><span class="attr">port</span> = <span class="number">71234</span></span><br><span class="line"><span class="attr">filter</span> = sshd</span><br><span class="line"><span class="attr">logpath</span> = /var/log/secure</span><br><span class="line"><span class="attr">maxretry</span> = <span class="number">3</span></span><br><span class="line"><span class="attr">findtime</span> = <span class="number">300</span></span><br><span class="line"><span class="attr">bantime</span> = <span class="number">3600</span></span><br><span class="line"></span><br><span class="line"><span class="section">[nginx-sensitive]</span></span><br><span class="line"><span class="attr">enabled</span> = <span class="literal">true</span></span><br><span class="line"><span class="attr">port</span> = http,https</span><br><span class="line"><span class="attr">filter</span> = nginx-sensitive</span><br><span class="line"><span class="attr">logpath</span> = /var/log/nginx/access.log</span><br><span class="line"><span class="attr">maxretry</span> = <span class="number">3</span></span><br><span class="line"><span class="attr">findtime</span> = <span class="number">300</span></span><br><span class="line"><span class="attr">bantime</span> = <span class="number">86400</span></span><br></pre></td></tr></table></figure><p>创建 Nginx 敏感路径过滤规则：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/fail2ban/filter.d/nginx-sensitive.conf</span><br></pre></td></tr></table></figure><p>比如</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Definition]</span></span><br><span class="line"><span class="attr">failregex</span> = ^&lt;HOST&gt; -.*<span class="string">&quot;(GET|POST).*(/xmlrpc\.php|/wp-config\.php|/\.git|/readme\.html|/license\.txt|/wp-admin/install\.php).*&quot;</span> .*</span><br><span class="line">ignoreregex =</span><br></pre></td></tr></table></figure><p>重启 Fail2ban：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl restart fail2ban</span><br></pre></td></tr></table></figure><p>查看状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">fail2ban-client status</span><br><span class="line">fail2ban-client status sshd</span><br><span class="line">fail2ban-client status nginx-sensitive</span><br></pre></td></tr></table></figure><hr><h3 id="1-7-配置定时任务监控服务器状态并异常报警"><a href="#1-7-配置定时任务监控服务器状态并异常报警" class="headerlink" title="1.7 配置定时任务监控服务器状态并异常报警"></a>1.7 配置定时任务监控服务器状态并异常报警</h3><hr><h2 id="2-Nginx-优化"><a href="#2-Nginx-优化" class="headerlink" title="2. Nginx 优化"></a>2. Nginx 优化</h2><h3 id="2-1-配置-SSL-证书"><a href="#2-1-配置-SSL-证书" class="headerlink" title="2.1 配置 SSL 证书"></a>2.1 配置 SSL 证书</h3><p>为了支持 HTTPS 访问，提高访问安全性，配置 SSL 证书并开启 HTTP 到 HTTPS 的跳转。</p><p>将证书文件放入指定目录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p /etc/nginx/ssl</span><br></pre></td></tr></table></figure><p>Nginx 配置示例：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span> <span class="number">80</span>;</span><br><span class="line">    <span class="attribute">server_name</span> threcial.cn www.threcial.cn;</span><br><span class="line">    <span class="attribute">return</span> <span class="number">301</span> https://<span class="variable">$host</span><span class="variable">$request_uri</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span> <span class="number">443</span> ssl http2;</span><br><span class="line">    <span class="attribute">server_name</span> threcial.cn www.threcial.cn;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">ssl_certificate</span> /etc/nginx/ssl/fullchain.pem;</span><br><span class="line">    <span class="attribute">ssl_certificate_key</span> /etc/nginx/ssl/privkey.pem;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">ssl_protocols</span> TLSv1.<span class="number">2</span> TLSv1.<span class="number">3</span>;</span><br><span class="line">    <span class="attribute">ssl_ciphers</span> HIGH:!aNULL:!MD5;</span><br><span class="line">    <span class="attribute">ssl_prefer_server_ciphers</span> <span class="literal">on</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">root</span> /www/wwwroot/threcial.cn;</span><br><span class="line">    <span class="attribute">index</span> index.php index.html index.htm;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>检查配置并重载：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nginx -t</span><br><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure><hr><h3 id="2-2-Nginx-层面禁止访问敏感路径"><a href="#2-2-Nginx-层面禁止访问敏感路径" class="headerlink" title="2.2 Nginx 层面禁止访问敏感路径"></a>2.2 Nginx 层面禁止访问敏感路径</h3><p>WordPress 经常被扫描的敏感文件包括 <code>xmlrpc.php</code>、<code>.git</code>、<code>wp-config.php</code>、<code>readme.html</code>、<code>license.txt</code>、备份文件等。通过 Nginx 直接拦截，减少请求进入 PHP 和 WordPress 的次数。</p><p>在站点配置中添加：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> = /xmlrpc.php &#123;</span><br><span class="line">    <span class="attribute">deny</span> all;</span><br><span class="line">    <span class="attribute">access_log</span> <span class="literal">off</span>;</span><br><span class="line">    <span class="attribute">log_not_found</span> <span class="literal">off</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="section">location</span> <span class="regexp">~* /(\.git|\.svn|\.env|\.DS_Store)</span> &#123;</span><br><span class="line">    <span class="attribute">deny</span> all;</span><br><span class="line">    <span class="attribute">access_log</span> <span class="literal">off</span>;</span><br><span class="line">    <span class="attribute">log_not_found</span> <span class="literal">off</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="section">location</span> <span class="regexp">~* /(wp-config\.php|readme\.html|license\.txt|composer\.(json|lock)|package\.json)</span> &#123;</span><br><span class="line">    <span class="attribute">deny</span> all;</span><br><span class="line">    <span class="attribute">access_log</span> <span class="literal">off</span>;</span><br><span class="line">    <span class="attribute">log_not_found</span> <span class="literal">off</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="section">location</span> <span class="regexp">~* \.(bak|old|backup|sql|tar|gz|zip|7z|rar)$</span> &#123;</span><br><span class="line">    <span class="attribute">deny</span> all;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>禁止上传目录执行 PHP：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> <span class="regexp">~* /wp-content/uploads/.*\.php$</span> &#123;</span><br><span class="line">    <span class="attribute">deny</span> all;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>检查并重载：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nginx -t</span><br><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure><hr><h3 id="2-3-配置静态资源缓存"><a href="#2-3-配置静态资源缓存" class="headerlink" title="2.3 配置静态资源缓存"></a>2.3 配置静态资源缓存</h3><p>为图片、CSS、JS、字体等静态资源设置浏览器缓存，减少重复请求，提高页面二次访问速度。</p><p>实际操作：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> <span class="regexp">~* \.(jpg|jpeg|png|gif|ico|webp|svg|css|js|woff|woff2|ttf|eot)$</span> &#123;</span><br><span class="line">    <span class="attribute">expires</span> <span class="number">30d</span>;</span><br><span class="line">    <span class="attribute">add_header</span> Cache-Control <span class="string">&quot;public, no-transform&quot;</span>;</span><br><span class="line">    <span class="attribute">access_log</span> <span class="literal">off</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>检查并重载：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nginx -t</span><br><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure><hr><h3 id="2-4-开启-Gzip-压缩"><a href="#2-4-开启-Gzip-压缩" class="headerlink" title="2.4 开启 Gzip 压缩"></a>2.4 开启 Gzip 压缩</h3><p>对文本类资源进行压缩传输，减少 HTML、CSS、JS、JSON 等文件体积。</p><p>实际操作：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">gzip</span> <span class="literal">on</span>;</span><br><span class="line"><span class="attribute">gzip_proxied</span> any;</span><br><span class="line"><span class="attribute">gzip_min_length</span> <span class="number">10k</span>;</span><br><span class="line"><span class="attribute">gzip_comp_level</span> <span class="number">6</span>;</span><br><span class="line"><span class="attribute">gzip_types</span> text/plain text/css application/xml application/json application/javascript text/javascript;</span><br></pre></td></tr></table></figure><p>检查配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nginx -t</span><br><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure><hr><h2 id="3-PHP-优化"><a href="#3-PHP-优化" class="headerlink" title="3. PHP 优化"></a>3. PHP 优化</h2><h3 id="3-1-调整-PHP-参数，降低单进程资源占用"><a href="#3-1-调整-PHP-参数，降低单进程资源占用" class="headerlink" title="3.1 调整 PHP 参数，降低单进程资源占用"></a>3.1 调整 PHP 参数，降低单进程资源占用</h3><p>WordPress 对 PHP 依赖较重，如果 PHP 参数过大，会导致轻量服务器内存占用过高。因此根据服务器配置调整 PHP 参数。</p><p>编辑 PHP 配置文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/php.ini</span><br></pre></td></tr></table></figure><p>示例配置：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">memory_limit</span> = <span class="number">128</span>M</span><br><span class="line"><span class="attr">max_execution_time</span> = <span class="number">60</span></span><br><span class="line"><span class="attr">max_input_time</span> = <span class="number">60</span></span><br><span class="line"><span class="attr">post_max_size</span> = <span class="number">32</span>M</span><br><span class="line"><span class="attr">upload_max_filesize</span> = <span class="number">32</span>M</span><br><span class="line"><span class="attr">max_file_uploads</span> = <span class="number">20</span></span><br></pre></td></tr></table></figure><p>重启 PHP-FPM：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl restart php-fpm</span><br></pre></td></tr></table></figure><p>验证配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">php -i | grep memory_limit</span><br><span class="line">php -i | grep max_execution_time</span><br></pre></td></tr></table></figure><hr><h3 id="3-2-优化-PHP-FPM-进程数量"><a href="#3-2-优化-PHP-FPM-进程数量" class="headerlink" title="3.2 优化 PHP-FPM 进程数量"></a>3.2 优化 PHP-FPM 进程数量</h3><p>根据轻量服务器内存大小，调整 PHP-FPM 子进程数量，避免并发过高时内存被打满。</p><p>实际操作：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/php-fpm.d/www.conf</span><br></pre></td></tr></table></figure><p>示例配置：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">pm</span> = dynamic</span><br><span class="line"><span class="attr">pm.max_children</span> = <span class="number">8</span></span><br><span class="line"><span class="attr">pm.start_servers</span> = <span class="number">2</span></span><br><span class="line"><span class="attr">pm.min_spare_servers</span> = <span class="number">1</span></span><br><span class="line"><span class="attr">pm.max_spare_servers</span> = <span class="number">4</span></span><br><span class="line"><span class="attr">pm.max_requests</span> = <span class="number">500</span></span><br></pre></td></tr></table></figure><p>重启服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl restart php-fpm</span><br></pre></td></tr></table></figure><p>查看 PHP-FPM 进程：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ps -ef | grep php-fpm</span><br></pre></td></tr></table></figure><hr><h3 id="3-3-开启-OPcache"><a href="#3-3-开启-OPcache" class="headerlink" title="3.3 开启 OPcache"></a>3.3 开启 OPcache</h3><p>开启 PHP OPcache，缓存 PHP 编译后的字节码，减少 WordPress 每次请求时重复编译 PHP 文件的开销。</p><p>实际操作：</p><p>编辑 OPcache 配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/php.d/10-opcache.ini</span><br></pre></td></tr></table></figure><p>示例配置：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">opcache.enable</span>=<span class="number">1</span></span><br><span class="line"><span class="attr">opcache.memory_consumption</span>=<span class="number">128</span></span><br><span class="line"><span class="attr">opcache.interned_strings_buffer</span>=<span class="number">8</span></span><br><span class="line"><span class="attr">opcache.max_accelerated_files</span>=<span class="number">10000</span></span><br><span class="line"><span class="attr">opcache.revalidate_freq</span>=<span class="number">60</span></span><br><span class="line"><span class="attr">opcache.validate_timestamps</span>=<span class="number">1</span></span><br></pre></td></tr></table></figure><p>重启 PHP-FPM：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl restart php-fpm</span><br></pre></td></tr></table></figure><p>验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">php -i | grep opcache.enable</span><br></pre></td></tr></table></figure><hr><h2 id="4-WordPress-优化"><a href="#4-WordPress-优化" class="headerlink" title="4. WordPress 优化"></a>4. WordPress 优化</h2><h3 id="4-1-修改-WordPress-登录页地址"><a href="#4-1-修改-WordPress-登录页地址" class="headerlink" title="4.1 修改 WordPress 登录页地址"></a>4.1 修改 WordPress 登录页地址</h3><p>WordPress 默认登录地址 <code>/wp-login.php</code> 和 <code>/wp-admin</code> 容易被扫描和爆破。通过插件或规则修改登录入口，减少无效请求。</p><p>在 WordPress 后台安装登录地址修改插件，例如 WPS Hide Login 类插件，设置新的登录地址：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">原地址：/wp-login.php</span><br><span class="line">新地址：/自定义登录路径</span><br></pre></td></tr></table></figure><p>同时在 Nginx 中限制默认登录入口访问：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> = /wp-login.php &#123;</span><br><span class="line">    <span class="attribute">deny</span> all;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果仍需保留部分访问，可以改为仅允许指定 IP：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> = /wp-login.php &#123;</span><br><span class="line">    <span class="attribute">allow</span> 你的固定IP;</span><br><span class="line">    <span class="attribute">deny</span> all;</span><br><span class="line">    <span class="attribute">include</span> fastcgi_params;</span><br><span class="line">    <span class="attribute">fastcgi_pass</span> unix:/run/php-fpm/www.sock;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>重载 Nginx：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nginx -t</span><br><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure><hr><h3 id="4-2-创建页面缓存，提高访问速度"><a href="#4-2-创建页面缓存，提高访问速度" class="headerlink" title="4.2 创建页面缓存，提高访问速度"></a>4.2 创建页面缓存，提高访问速度</h3><p>WordPress 动态页面每次访问都需要经过 Nginx、PHP、数据库和主题渲染。通过页面缓存，将动态页面生成静态 HTML，提高访问速度。</p><p>实际操作：</p><p>在 WordPress 后台安装并启用缓存插件，例如 WP Super Cache、W3 Total Cache 或 LiteSpeed Cache。</p><p>以页面缓存为例，开启：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">启用缓存功能</span><br><span class="line">启用页面静态化</span><br><span class="line">启用移动端缓存</span><br><span class="line">启用已知用户不缓存</span><br><span class="line">启用压缩页面</span><br><span class="line">设置缓存过期时间</span><br></pre></td></tr></table></figure><p>检查缓存目录是否生成缓存文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> -lh wp-content/cache/</span><br></pre></td></tr></table></figure><p>如果使用 Nginx，需要确认伪静态和缓存规则正常，不要让缓存插件生成的规则与 Nginx 配置冲突。</p><hr><h3 id="4-3-创建本地字体库"><a href="#4-3-创建本地字体库" class="headerlink" title="4.3 创建本地字体库"></a>4.3 创建本地字体库</h3><p>主题或插件默认可能从外部 CDN 加载字体，国内访问容易变慢。将字体文件下载到本地服务器，由本地 Nginx 提供访问。</p><p>创建字体目录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p /www/wwwroot/threcial.cn/wp-content/uploads/fonts</span><br></pre></td></tr></table></figure><p>上传字体文件，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">woff</span><br><span class="line">woff2</span><br><span class="line">ttf</span><br></pre></td></tr></table></figure><p>在主题 CSS 或自定义 CSS 中引用本地字体：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">@font-face</span> &#123;</span><br><span class="line">    <span class="attribute">font-family</span>: <span class="string">&#x27;LocalFont&#x27;</span>;</span><br><span class="line">    <span class="attribute">src</span>: <span class="built_in">url</span>(<span class="string">&#x27;/wp-content/uploads/fonts/font.woff2&#x27;</span>) <span class="built_in">format</span>(<span class="string">&#x27;woff2&#x27;</span>);</span><br><span class="line">    <span class="attribute">font-weight</span>: normal;</span><br><span class="line">    <span class="attribute">font-style</span>: normal;</span><br><span class="line">    <span class="attribute">font-display</span>: swap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>设置字体文件缓存：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> <span class="regexp">~* \.(woff|woff2|ttf|eot)$</span> &#123;</span><br><span class="line">    <span class="attribute">expires</span> <span class="number">30d</span>;</span><br><span class="line">    <span class="attribute">add_header</span> Cache-Control <span class="string">&quot;public&quot;</span>;</span><br><span class="line">    <span class="attribute">access_log</span> <span class="literal">off</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="5-优化总结"><a href="#5-优化总结" class="headerlink" title="5. 优化总结"></a>5. 优化总结</h2><p>本次优化主要从系统层、Nginx 层、PHP 层和 WordPress 层进行处理。</p><p>系统层面：<br>增加 Swap，配置防火墙，修改 SSH 端口，关闭密码登录，限制 SSH 连接数，并使用 Fail2ban 自动封禁异常 IP，提高服务器稳定性和基础安全性。</p><p>Nginx 层面：<br>配置 HTTPS，拦截敏感路径，禁止访问高风险文件，开启静态资源缓存和 Gzip 压缩，减少恶意请求和重复资源加载。</p><p>PHP 层面：<br>调整 PHP 参数和 PHP-FPM 进程数量，开启 OPcache，减少 PHP 资源占用，提高 WordPress 动态页面处理效率。</p><p>WordPress 层面：<br>修改后台登录地址，配置页面缓存，创建本地字体库，并优化 Sakurairo 主题静态资源，减少数据库查询和外部资源请求。</p><p>整体效果：<br>网站访问速度提升，移动端加载更稳定，服务器资源占用降低，同时增强了对 SSH 爆破、WordPress 扫描和敏感路径访问的防护能力。</p>]]>
    </content>
    <id>https://threcial.github.io/linux/%E7%BD%91%E7%AB%99%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E8%AE%B0%E5%BD%95/</id>
    <link href="https://threcial.github.io/linux/%E7%BD%91%E7%AB%99%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E8%AE%B0%E5%BD%95/"/>
    <published>2026-07-03T07:51:56.000Z</published>
    <summary>网站性能优化记录 服务器环境：Linux、Nginx、PHP、WordPress、Sakurairo 主题 服务器类型：轻量应用服务器 优化目标：提高网站访问速度、降低资源占用、增强基础安全防护、减少</summary>
    <title>网站性能优化记录</title>
    <updated>2026-07-03T07:51:56.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="docker" scheme="https://threcial.github.io/categories/docker/"/>
    <category term="docker" scheme="https://threcial.github.io/tags/docker/"/>
    <category term="虚拟化" scheme="https://threcial.github.io/tags/%E8%99%9A%E6%8B%9F%E5%8C%96/"/>
    <content>
      <![CDATA[<p>Docker 把应用和运行环境打包成镜像，再把镜像运行成容器</p><p>镜像是静态模板，容器是运行中的进程</p><h2 id="Docker-与虚拟机"><a href="#Docker-与虚拟机" class="headerlink" title="Docker 与虚拟机"></a>Docker 与虚拟机</h2><table><thead><tr><th>对比项</th><th>容器</th><th>虚拟机</th></tr></thead><tbody><tr><td>内核</td><td>共享宿主机内核</td><td>每台虚拟机有完整系统内核</td></tr><tr><td>启动速度</td><td>秒级或更快</td><td>通常分钟级</td></tr><tr><td>资源占用</td><td>较低</td><td>较高</td></tr><tr><td>隔离层次</td><td>操作系统进程级隔离</td><td>硬件虚拟化隔离</td></tr><tr><td>适用场景</td><td>应用交付、环境一致性、弹性部署</td><td>强隔离、多系统运行</td></tr></tbody></table><p>Docker 的核心收益是环境一致、迁移方便、启动快、资源利用率高。与其说是“小型虚拟机”，不如说是“带隔离环境的进程”</p><h2 id="安装-Docker"><a href="#安装-Docker" class="headerlink" title="安装 Docker"></a>安装 Docker</h2><p>CentOS 7 安装示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">yum remove -y docker*</span><br><span class="line">yum install -y yum-utils</span><br><span class="line">yum-config-manager --add-repo http://mirrors.aliyun.com/dockerce/linux/centos/docker-ce.repo</span><br><span class="line">yum install -y docker-ce docker-ce-cli containerd.io</span><br></pre></td></tr></table></figure><h2 id="镜像操作"><a href="#镜像操作" class="headerlink" title="镜像操作"></a>镜像操作</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">docker images</span><br><span class="line">docker image ls</span><br><span class="line">docker image ls -q</span><br><span class="line"></span><br><span class="line">docker pull redis:6.0</span><br><span class="line">docker pull nginx</span><br><span class="line"></span><br><span class="line">docker rmi nginx:latest</span><br><span class="line">docker image rm redis:6.0</span><br><span class="line"></span><br><span class="line">docker search redis</span><br></pre></td></tr></table></figure><p>查看镜像分层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker history nginx</span><br><span class="line">docker history nginx --format &quot;table &#123;.ID&#125;&#123;.CreatedBy&#125;&quot; --no-trunc</span><br></pre></td></tr></table></figure><h2 id="容器运行"><a href="#容器运行" class="headerlink" title="容器运行"></a>容器运行</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">docker run nginx:latest</span><br><span class="line">docker run -d -p 1192:80 nginx:latest</span><br><span class="line">docker run -d -p 1721:80 --name nginx01 nginx:latest</span><br><span class="line">docker run -it nginx:latest bash</span><br></pre></td></tr></table></figure><p>参数：</p><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-d</code></td><td>后台运行</td></tr><tr><td><code>-p 宿主机端口:容器端口</code></td><td>端口映射</td></tr><tr><td><code>-P</code></td><td>随机映射镜像声明端口</td></tr><tr><td><code>--name</code></td><td>指定容器名</td></tr><tr><td><code>-it</code></td><td>交互终端</td></tr><tr><td><code>--restart=always</code></td><td>容器异常退出或 Docker 重启后自动拉起</td></tr></tbody></table><p>容器启动后必须有前台进程存在。如果容器内没有前台进程那么容器就退出</p><h2 id="查看、启停、删除容器"><a href="#查看、启停、删除容器" class="headerlink" title="查看、启停、删除容器"></a>查看、启停、删除容器</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">docker ps</span><br><span class="line">docker ps -a</span><br><span class="line">docker ps -aq</span><br><span class="line"></span><br><span class="line">docker start nginx01</span><br><span class="line">docker stop nginx01</span><br><span class="line">docker restart nginx01</span><br><span class="line">docker kill nginx01</span><br><span class="line">docker rm nginx01</span><br><span class="line">docker rm -f nginx01</span><br><span class="line">docker rm -f $(docker ps -qa)</span><br></pre></td></tr></table></figure><p>已经启动的容器配置自启：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker update --restart=always 容器ID</span><br></pre></td></tr></table></figure><p>取消自启：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker update --restart=no 容器ID</span><br></pre></td></tr></table></figure><h2 id="进入容器与执行命令"><a href="#进入容器与执行命令" class="headerlink" title="进入容器与执行命令"></a>进入容器与执行命令</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker exec -it nginx01 bash</span><br><span class="line">docker exec nginx01 nginx -t</span><br></pre></td></tr></table></figure><p><code>docker exec</code> 是进入已运行容器的常用方式。<code>docker run -it image bash</code> 是新建一个容器并进入，退出后容器也退出</p><h2 id="宿主机与容器复制文件"><a href="#宿主机与容器复制文件" class="headerlink" title="宿主机与容器复制文件"></a>宿主机与容器复制文件</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">docker cp nginx01:/usr/share/nginx/html/index.html /root/</span><br><span class="line"></span><br><span class="line">docker cp /root/test.html nginx01:/usr/share/nginx/html/</span><br></pre></td></tr></table></figure><p><code>docker cp</code> 用于临时拷贝</p><p>长期同步代码、配置或数据时，使用 volume 或 bind mount</p><h2 id="容器日志"><a href="#容器日志" class="headerlink" title="容器日志"></a>容器日志</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">docker logs nginx01</span><br><span class="line"></span><br><span class="line">docker inspect nginx01 | less</span><br></pre></td></tr></table></figure><h2 id="commit、save、load"><a href="#commit、save、load" class="headerlink" title="commit、save、load"></a>commit、save、load</h2><p>把容器提交为镜像：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker commit -m &quot;ngx-test&quot; -a &quot;xwx&quot; nginx01 ngx-01:latest</span><br></pre></td></tr></table></figure><p>保存镜像为 tar：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker save ngx-01:latest -o ngx-01.tar</span><br></pre></td></tr></table></figure><p>导入镜像：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker load -i ngx-01.tar</span><br></pre></td></tr></table></figure><h2 id="数据卷"><a href="#数据卷" class="headerlink" title="数据卷"></a>数据卷</h2><p>数据卷用于把数据从容器生命周期中分离出来，删除容器时不会自动删除命名数据卷</p><h3 id="bind-mount"><a href="#bind-mount" class="headerlink" title="bind mount"></a>bind mount</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -d -P -v /html:/usr/share/nginx/html nginx:latest</span><br></pre></td></tr></table></figure><p>宿主机目录直接映射到容器目录。注意：宿主机目录内容会覆盖容器目标目录</p><h3 id="named-volume"><a href="#named-volume" class="headerlink" title="named volume"></a>named volume</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker volume create web001</span><br><span class="line">docker run -d -p 3333:80 --name nginx20 -v web001:/usr/share/nginx/html nginx</span><br></pre></td></tr></table></figure><p>查看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker volume ls</span><br><span class="line">docker volume inspect web001</span><br></pre></td></tr></table></figure><p>删除：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker volume rm web001</span><br><span class="line">docker volume prune</span><br></pre></td></tr></table></figure><p><strong><code>docker volume prune</code> 会删除未被容器使用的数据卷，执行前必须确认没有误删风险</strong></p><h2 id="Docker-网络"><a href="#Docker-网络" class="headerlink" title="Docker 网络"></a>Docker 网络</h2><p>安装 Docker 后会自动创建 <code>docker0</code> 网桥。默认 bridge 网络中，容器通过 veth pair 接入 docker0，再通过 NAT 访问外部</p><table><thead><tr><th>网络模式</th><th>说明</th><th>参数</th></tr></thead><tbody><tr><td>bridge</td><td>默认模式，每个容器有独立 IP</td><td><code>--network bridge</code></td></tr><tr><td>host</td><td>使用宿主机网络命名空间</td><td><code>--network host</code></td></tr><tr><td>none</td><td>不配置网络</td><td><code>--network none</code></td></tr><tr><td>container</td><td>与另一个容器共享网络</td><td><code>--network container:容器名</code></td></tr></tbody></table><p>查看网络：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker network ls</span><br><span class="line">docker network inspect bridge</span><br></pre></td></tr></table></figure><p>创建用户自定义网络：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">docker network create webnet</span><br><span class="line">docker run -d --name nginx01 --network webnet nginx</span><br><span class="line">docker run -d --name nginx02 --network webnet nginx</span><br></pre></td></tr></table></figure><p>在用户自定义 bridge 网络里，容器可以直接用容器名互相解析</p><h2 id="部署常见服务"><a href="#部署常见服务" class="headerlink" title="部署常见服务"></a>部署常见服务</h2><h3 id="Redis"><a href="#Redis" class="headerlink" title="Redis"></a>Redis</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -d --name redis   -p 6379:6379   -v redis_data:/data   redis:5.0.7 redis-server --appendonly yes</span><br></pre></td></tr></table></figure><h3 id="Nginx"><a href="#Nginx" class="headerlink" title="Nginx"></a>Nginx</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -d --name nginx   -p 80:80   -v /html:/usr/share/nginx/html   nginx:latest</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/docker/docker%E5%9F%BA%E7%A1%80/</id>
    <link href="https://threcial.github.io/docker/docker%E5%9F%BA%E7%A1%80/"/>
    <published>2026-06-15T07:10:25.000Z</published>
    <summary>Docker 把应用和运行环境打包成镜像，再把镜像运行成容器 镜像是静态模板，容器是运行中的进程 Docker 与虚拟机 对比项容器虚拟机内核共享宿主机内核每台虚拟机有完整系统内核启动速度秒级或更快通</summary>
    <title>Docker基础</title>
    <updated>2026-06-15T07:10:25.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="数据库" scheme="https://threcial.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    <category term="mysql" scheme="https://threcial.github.io/tags/mysql/"/>
    <category term="主从复制" scheme="https://threcial.github.io/tags/%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6/"/>
    <category term="备份恢复" scheme="https://threcial.github.io/tags/%E5%A4%87%E4%BB%BD%E6%81%A2%E5%A4%8D/"/>
    <content>
      <![CDATA[<p>MySQL 备份方式可以分类</p><p>按备份方式分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">逻辑备份：导出 SQL 语句，比如 mysqldump</span><br><span class="line">物理备份：直接备份数据文件，比如 xtrabackup</span><br></pre></td></tr></table></figure><p>按备份范围分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">全量备份：某个时间点完整导出一份数据</span><br><span class="line">增量备份：只记录全量备份之后发生的变化</span><br></pre></td></tr></table></figure><p>本文主要讲逻辑备份，主从复制也是基于逻辑备份</p><h2 id="mysqldump：全量备份"><a href="#mysqldump：全量备份" class="headerlink" title="mysqldump：全量备份"></a>mysqldump：全量备份</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysqldump -u root -p --single-transaction --master-data=2 -B rocky &gt;rocky.sql</span><br></pre></td></tr></table></figure><p><code>--single-transaction</code> 会在备份开始时开启一个事务，这样可以保证备份一致性。<code>-x</code> 会使用锁死表的方式来保证一致性，但这样会导致前端业务卡住，所以不推荐使用 <code>-x</code></p><p><code>--master-data=2</code> 会在备份文件里以注释的方式记录 binlog 的位置，便于恢复时使用</p><p><code>-B</code> 会将后面所有参数视为库，否则只有第一参数为库，后面的参数都为表</p><h2 id="binlog：增量备份"><a href="#binlog：增量备份" class="headerlink" title="binlog：增量备份"></a>binlog：增量备份</h2><p><code>mysqldump</code> 只能恢复到备份那个时间点，如果凌晨 2 点做了全量备份，下午 5 点误删数据，那么只靠凌晨 2 点的备份，最多恢复到 2 点，2 点到 5 点之间的写入就丢了</p><p>这时候就需要 binlog</p><p>binlog 是 MySQL 的二进制日志，记录数据库中除了查询以外的变更操作</p><p>开启 binlog，需要在 <code>my.cnf</code> 里配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line"></span><br><span class="line">server-id=1                               标识，用于主从复制使用</span><br><span class="line">log-bin=/data/mysql-bin/binlog             备份文件前缀，结尾的binlog不是目录</span><br><span class="line">binlog_format=ROW                          记录方式</span><br><span class="line">expire_logs_days=7                         过期时间</span><br><span class="line">max_binlog_size=500M                        文件大小</span><br><span class="line">sync_binlog=0                               同步方式</span><br><span class="line"></span><br><span class="line">log-bin 指定 binlog 文件目录和前缀</span><br><span class="line">binlog 目录必须存在</span><br><span class="line">目录用户和用户组要改成 mysql</span><br><span class="line">server-id 必须配置，否则 binlog复制相关配置可能不正常</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sync_binlog=0：由操作系统决定什么时候刷盘，性能好，安全性弱一些</span><br><span class="line">sync_binlog=1：每次事务提交都刷 binlog，安全性高，性能下降</span><br><span class="line">sync_binlog=N：每 N 次事务提交刷一次</span><br></pre></td></tr></table></figure><h2 id="mysqlbinlog：把-binlog-转成可恢复的-SQL"><a href="#mysqlbinlog：把-binlog-转成可恢复的-SQL" class="headerlink" title="mysqlbinlog：把 binlog 转成可恢复的 SQL"></a>mysqlbinlog：把 binlog 转成可恢复的 SQL</h2><p>binlog 是二进制文件，不能直接 <code>cat</code> 看。要用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysqlbinlog binlog.000001</span><br></pre></td></tr></table></figure><p>如果遇到字符集配置报错，可以加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysqlbinlog --no-defaults binlog.000001</span><br></pre></td></tr></table></figure><p>常用参数：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">-d                  指定数据库</span><br><span class="line">-r                  输出到文件</span><br><span class="line">--start-position    指定开始位置</span><br><span class="line">--stop-position     指定结束位置</span><br><span class="line">--start-datetime    指定开始时间</span><br><span class="line">--stop-datetime     指定结束时间</span><br><span class="line">--no-defaults       不读取默认配置文件，避免客户端配置影响 mysqlbinlog</span><br><span class="line">--skip-gtids        跳过 GTID 信息，某些场景配合 -d 使用</span><br></pre></td></tr></table></figure><p>把 binlog 转成 SQL 文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysqlbinlog --no-defaults binlog.000016 -r incr.sql</span><br></pre></td></tr></table></figure><p>多个 binlog 文件一起处理：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">mysqlbinlog --no-defaults </span><br><span class="line">  binlog.000016 binlog.000017 </span><br><span class="line">  -d rocky </span><br><span class="line">  --skip-gtids </span><br><span class="line">  --start-position=2725 </span><br><span class="line">  -r rocky_incr.sql</span><br></pre></td></tr></table></figure><p>按时间：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">mysqlbinlog --no-defaults </span><br><span class="line">  --start-datetime=&#x27;2026-06-04 13:02:00&#x27; </span><br><span class="line">  --stop-datetime=&#x27;2026-06-04 13:30:00&#x27; </span><br><span class="line">  binlog.000016 </span><br><span class="line">  -r incr.sql</span><br></pre></td></tr></table></figure><h2 id="备份文件校验：md5sum"><a href="#备份文件校验：md5sum" class="headerlink" title="备份文件校验：md5sum"></a>备份文件校验：md5sum</h2><p>备份文件生成之后还要确认传输过程没有损坏</p><p>生成校验文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">md5sum rocky.sql &gt;rocky.sql.flag</span><br></pre></td></tr></table></figure><p>传到远端后，在同目录验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">md5sum -c rocky.sql.flag</span><br></pre></td></tr></table></figure><h2 id="备份恢复流程"><a href="#备份恢复流程" class="headerlink" title="备份恢复流程"></a>备份恢复流程</h2><p>假设有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">全量备份：rocky_2026-06-04_02-00-00.sql</span><br><span class="line">binlog：mysql8.000008、mysql8.000009</span><br><span class="line">全量备份里记录的起点：mysql8.000008:478</span><br></pre></td></tr></table></figure><p><strong>恢复时先隔离业务</strong>：关闭应用连接；禁止继续写入；只允许本机操作数据库；确认恢复目标库和当前库状态</p><p>生成增量 SQL：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">mysqlbinlog --no-defaults</span><br><span class="line">  mysql8.000008 mysql8.000009 </span><br><span class="line">  -d rocky </span><br><span class="line">  --skip-gtids </span><br><span class="line">  --start-position=478 </span><br><span class="line">  -r rocky_incr.sql</span><br></pre></td></tr></table></figure><p>恢复全量：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql -u root -p &lt; rocky<span class="emphasis">_2026-06-04_</span>02-00-00.sql</span><br></pre></td></tr></table></figure><p>再恢复增量：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql -u root -p &lt; rocky<span class="emphasis">_incr.sql</span></span><br></pre></td></tr></table></figure><p>恢复完成后验证数据</p><p>恢复过程本身会产生大量 <code>INSERT/UPDATE/CREATE</code>，如果 binlog 开着，这些恢复动作也会被重新写入 binlog，需要根据实际情况考虑是否临时关闭 binlog</p><h2 id="主从复制"><a href="#主从复制" class="headerlink" title="主从复制"></a>主从复制</h2><p>主从复制其实就是把“binlog 增量恢复”自动化</p><p>主从复制原理是：从库自动连接主库自动拉取主库 binlog，写成自己的 relay log，再由 SQL 线程自动重放</p><h3 id="主从复制作用"><a href="#主从复制作用" class="headerlink" title="主从复制作用"></a>主从复制作用</h3><p>读写分离：主库写，从库读<br>故障切换：主库故障时，从库可以提升为新主库<br>报表查询：复杂查询放到从库，避免影响主库</p><p><strong>主从复制不是备份</strong>，如果主库执行 update 从库也会执行</p><h3 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h3><p>假设：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">主库：192.168.174.128</span><br><span class="line">从库：192.168.174.132</span><br><span class="line">同步库：rocky</span><br><span class="line">主库用于复制的用户：repli</span><br></pre></td></tr></table></figure><p>主库<code>my.cnf</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">server-id=1</span><br><span class="line">log-bin=/data/mysql-bin/mysql8</span><br><span class="line">binlog_format=ROW</span><br></pre></td></tr></table></figure><p>主库用户 repli：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CREATE USER &#x27;repli&#x27;@&#x27;192.168.174.%&#x27; IDENTIFIED BY &#x27;repli&#x27;;</span><br><span class="line">GRANT REPLICATION SLAVE ON *.* TO &#x27;repli&#x27;@&#x27;192.168.174.%&#x27;;</span><br><span class="line">FLUSH PRIVILEGES;</span><br></pre></td></tr></table></figure><p>主库导出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysqldump -u root -p --single-transaction --master-data=2 -B rocky &gt;rocky.sql</span><br></pre></td></tr></table></figure><p>导出后找坐标：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">grep -i &quot;CHANGE MASTER&quot; rocky.sql</span><br><span class="line"></span><br><span class="line">-- CHANGE MASTER TO MASTER_LOG_FILE=&#x27;mysql8.000008&#x27;, MASTER_LOG_POS=478; </span><br><span class="line">得到从库起点</span><br></pre></td></tr></table></figure><p>从库 <code>my.cnf</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">server-id=2</span><br><span class="line">relay_log=mysql-relay-bin</span><br><span class="line">read_only=ON</span><br><span class="line">super_read_only=ON</span><br></pre></td></tr></table></figure><p>如果这个从库不会再作为其他从库的主库，可以不开 binlog</p><p>如果只同步某个库，可以配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">replicate-do-db=rocky</span><br></pre></td></tr></table></figure><p>从库导入主库全量备份：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql -u root -p &lt; rocky.sql</span><br></pre></td></tr></table></figure><p>从库登录 MySQL ：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">CHANGE REPLICATION SOURCE TO</span><br><span class="line">SOURCE_HOST=&#x27;192.168.174.128&#x27;,</span><br><span class="line">SOURCE_PORT=3306,</span><br><span class="line">SOURCE_USER=&#x27;repli&#x27;,</span><br><span class="line">SOURCE_PASSWORD=&#x27;repli&#x27;,</span><br><span class="line">SOURCE_LOG_FILE=&#x27;mysql8.000008&#x27;,</span><br><span class="line">SOURCE_LOG_POS=478,</span><br><span class="line">GET_SOURCE_PUBLIC_KEY=1;</span><br><span class="line"></span><br><span class="line">START REPLICA;</span><br><span class="line">SHOW REPLICA STATUS;</span><br></pre></td></tr></table></figure><p>旧版本写法：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">CHANGE MASTER TO</span><br><span class="line">MASTER_HOST=&#x27;192.168.174.128&#x27;,</span><br><span class="line">MASTER_PORT=3306,</span><br><span class="line">MASTER_USER=&#x27;repli&#x27;,</span><br><span class="line">MASTER_PASSWORD=&#x27;repli&#x27;,</span><br><span class="line">MASTER_LOG_FILE=&#x27;mysql8.000008&#x27;,</span><br><span class="line">MASTER_LOG_POS=478,</span><br><span class="line">GET_MASTER_PUBLIC_KEY=1;</span><br><span class="line"></span><br><span class="line">START SLAVE;</span><br><span class="line">SHOW SLAVE STATUS;</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">STOP REPLICA;                停止复制</span><br><span class="line">STOP SLAVE;</span><br><span class="line"></span><br><span class="line">RESET REPLICA ALL;        删除复制配置</span><br><span class="line">RESET SLAVE ALL;</span><br></pre></td></tr></table></figure><p><strong>主从复制默认是异步的。主库提交成功，不代表从库已经执行完成</strong></p><p>延迟常见原因：从库太多，主库网络和发送压力变大；从库硬件弱于主库；主库写入压力过高等</p>]]>
    </content>
    <id>https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/mysql-%E5%A4%87%E4%BB%BD%E6%81%A2%E5%A4%8D%E4%B8%8E%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6/</id>
    <link href="https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/mysql-%E5%A4%87%E4%BB%BD%E6%81%A2%E5%A4%8D%E4%B8%8E%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6/"/>
    <published>2026-06-07T02:22:41.000Z</published>
    <summary>MySQL 备份方式可以分类 按备份方式分： 逻辑备份：导出 SQL 语句，比如 mysqldump 物理备份：直接备份数据文件，比如 xtrabackup 按备份范围分： 全量备份：某个时间点完整导</summary>
    <title>MySQL 备份恢复与主从复制</title>
    <updated>2026-06-07T02:22:41.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="数据库" scheme="https://threcial.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    <category term="mysql" scheme="https://threcial.github.io/tags/mysql/"/>
    <category term="函数" scheme="https://threcial.github.io/tags/%E5%87%BD%E6%95%B0/"/>
    <category term="索引" scheme="https://threcial.github.io/tags/%E7%B4%A2%E5%BC%95/"/>
    <category term="触发器" scheme="https://threcial.github.io/tags/%E8%A7%A6%E5%8F%91%E5%99%A8/"/>
    <content>
      <![CDATA[<p><strong>触发器</strong>：在 <code>insert/update/delete</code> 前后自动执行动作<br><strong>索引</strong>：减少扫描范围，提高查询效率</p><h2 id="常用函数"><a href="#常用函数" class="headerlink" title="常用函数"></a>常用函数</h2><p><strong>聚合函数</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">COUNT(col)   统计查询结果的行数</span><br><span class="line">MIN(col)   查询指定列的最小值</span><br><span class="line">MAX(col)   查询指定列的最大值</span><br><span class="line">SUM(col)   求和，返回指定列的总和</span><br><span class="line">AVG(col)   求平均值，返回指定列数据的平均值</span><br></pre></td></tr></table></figure><p><strong>数值型函数</strong></p><p>数值型函数主要是对数值型数据进行处理</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">ABS(x)                 返回x的绝对值</span><br><span class="line">BIN(x)                返回x的二进制</span><br><span class="line">CEILING(x)            返回大于x的最小整数值</span><br><span class="line">EXP(x)                返回值e（自然对数的底）的x次方</span><br><span class="line">FLOOR(x)                返回小于x的最大整数值</span><br><span class="line">GREATEST(x1,x2,...,xn)   返回集合中最大的值</span><br><span class="line">LEAST(x1,x2,...,xn)   返回集合中最小的值</span><br><span class="line">LN(x)                 返回x的自然对数</span><br><span class="line">LOG(x,y)               返回x的以y为底的对数</span><br><span class="line">MOD(x,y)               返回x/y的模（余数）</span><br><span class="line">PI()                  返回pi的值（圆周率）</span><br><span class="line">RAND()                返回０到１内的随机值,可以通过提供一个参数(种子)使RAND()随机数生成器生成一个指定的值</span><br><span class="line">ROUND(x,y)            返回参数x的四舍五入的有y位小数的值</span><br><span class="line">TRUNCATE(x,y)         返回数字x截短为y位小数的结果</span><br></pre></td></tr></table></figure><h4 id="字符串函数"><a href="#字符串函数" class="headerlink" title="字符串函数"></a>字符串函数</h4><p>字符串函数可以对字符串类型数据进行处理</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">LENGTH(s)              计算字符串长度函数，返回字符串的字节长度</span><br><span class="line">CONCAT(s1,s2...,sn)   合并字符串函数，返回结果为连接参数产生的字符串，参数可以是一个或多个</span><br><span class="line">INSERT(str,x,y,instr)   将字符串str从第x位置开始，y个字符长的子串替换为字符串instr，返回结果</span><br><span class="line">LOWER(str)              将字符串中的字母转换为小写</span><br><span class="line">UPPER(str)             将字符串中的字母转换为大写</span><br><span class="line">LEFT(str,x)             返回字符串str中最左边的x个字符</span><br><span class="line">RIGHT(str,x)             返回字符串str中最右边的x个字符</span><br><span class="line">TRIM(str)               删除字符串左右两侧的空格</span><br><span class="line">REPLACE               字符串替换函数，返回替换后的新字符串</span><br><span class="line">SUBSTRING              截取字符串，返回从指定位置开始的指定长度的字符换</span><br><span class="line">REVERSE(str)           返回颠倒字符串str的结果</span><br></pre></td></tr></table></figure><p><strong>日期和时间函数</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CURDATE             返回当前系统的日期值</span><br><span class="line">CURTIME            返回当前系统的时间值</span><br><span class="line">NOW                返回当前系统的日期和时间值</span><br><span class="line">UNIX_TIMESTAMP   获取UNIX时间戳函数，返回一个以 UNIX 时间戳为基础的无符号整数</span><br></pre></td></tr></table></figure><p><strong>加密函数</strong></p><p>加密函数主要用于对字符串进行加密</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ENCRYPT(str,salt)   使用UNIXcrypt()函数，用关键词salt(一个可以惟一确定口令的字符串，就像钥匙一样)加密字符串str</span><br><span class="line">MD5()              计算字符串str的MD5校验和</span><br></pre></td></tr></table></figure><h2 id="触发器"><a href="#触发器" class="headerlink" title="触发器"></a>触发器</h2><p>触发器是和表绑定的对象，在 <code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code> 前后自动执行一段 SQL</p><p>适合用做数据校验、日志记录、审计记录等。MySQL 触发器只支持行级触发，不支持语句级触发。</p><p>基本语法：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">CREATE TRIGGER trigger_name</span><br><span class="line">BEFORE | AFTER INSERT | UPDATE | DELETE</span><br><span class="line">ON tb_name</span><br><span class="line">FOR EACH ROW</span><br><span class="line">BEGIN</span><br><span class="line">    trigger_statement;</span><br><span class="line">END;</span><br></pre></td></tr></table></figure><p>操作触发器：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SHOW TRIGGERS;</span><br><span class="line">DROP TRIGGER IF EXISTS trigger_name;</span><br></pre></td></tr></table></figure><p>触发器里用 <code>NEW</code> 和 <code>OLD</code> 引用变化前后的数据</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">INSERT：只有 NEW，表示即将插入或刚插入的新记录</span><br><span class="line">DELETE：只有 OLD，表示即将删除或刚删除的旧记录</span><br><span class="line">UPDATE：既有 OLD，也有 NEW，OLD 是旧值，NEW 是新值</span><br></pre></td></tr></table></figure><h3 id="索引：减少扫描范围"><a href="#索引：减少扫描范围" class="headerlink" title="索引：减少扫描范围"></a>索引：减少扫描范围</h3><p>索引的核心作用是减少 MySQL 需要扫描的数据量</p><p>没有索引时，查一条数据可能要从头扫到尾；有合适索引时，可以沿着索引结构快速定位范围</p><p>但索引并不是越多越好，因为索引也要占用额外磁盘空间；INSERT、UPDATE、DELETE 时索引也要维护；重复值很多的列建索引效果差；小表很多时候全表扫描更快</p><p><strong>索引是为查询服务的，但会拖慢写入和更新</strong></p><h3 id="常见索引"><a href="#常见索引" class="headerlink" title="常见索引"></a>常见索引</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE user ADD INDEX idx_name(name);               普通索引</span><br><span class="line">ALTER TABLE user ADD UNIQUE uk_email(email);            唯一索引</span><br><span class="line">CREATE INDEX idx_name_age ON user(name, age);            联合索引</span><br><span class="line">CREATE INDEX idx_title_prefix ON article(title(20));       短索引</span><br><span class="line">ALTER TABLE user DROP INDEX idx_name;                      删除索引</span><br><span class="line">SHOW INDEX FROM user;                                     查看表索引</span><br></pre></td></tr></table></figure><h3 id="适合建索引的列"><a href="#适合建索引的列" class="headerlink" title="适合建索引的列"></a>适合建索引的列</h3><p>经常作为搜索条件；经常用于连接；经常用于范围查询；经常用于排序；经常用于分组；唯一性要求高的字段，适合唯一索引</p><h3 id="不适合建索引的列"><a href="#不适合建索引的列" class="headerlink" title="不适合建索引的列"></a>不适合建索引的列</h3><p>不常出现在查询条件里的列，不适合建索引</p><p>重复值太多的列不适合建索引。比如性别字段只有男、女，如果单独建索引，过滤效果通常很差</p><p>大文本字段不适合直接建普通索引</p><p>写入特别频繁、查询很少的列，也不适合加索引</p><p><strong>查询中很少使用的列、重复值多的列、text&#x2F;image&#x2F;bit 这类数据类型、修改性能要求远高于检索性能的列，不应该随便加索引</strong></p><h3 id="联合索引之最左前缀"><a href="#联合索引之最左前缀" class="headerlink" title="联合索引之最左前缀"></a>联合索引之最左前缀</h3><p>最左前缀原则：联合索引从最左列开始连续匹配，中间断了，后面的列就难以继续按索引顺序使用</p>]]>
    </content>
    <id>https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/mysql-%E5%B8%B8%E7%94%A8%E5%87%BD%E6%95%B0%E3%80%81%E8%A7%A6%E5%8F%91%E5%99%A8%E3%80%81%E7%B4%A2%E5%BC%95/</id>
    <link href="https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/mysql-%E5%B8%B8%E7%94%A8%E5%87%BD%E6%95%B0%E3%80%81%E8%A7%A6%E5%8F%91%E5%99%A8%E3%80%81%E7%B4%A2%E5%BC%95/"/>
    <published>2026-06-06T12:40:56.000Z</published>
    <summary>触发器：在 insert/update/delete 前后自动执行动作 索引：减少扫描范围，提高查询效率 常用函数 聚合函数 COUNT(col) 统计查询结果的行数 MIN(col) 查询指定列的最</summary>
    <title>MySQL 常用函数、触发器、索引</title>
    <updated>2026-06-06T12:40:56.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="数据库" scheme="https://threcial.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    <category term="mysql" scheme="https://threcial.github.io/tags/mysql/"/>
    <category term="事务" scheme="https://threcial.github.io/tags/%E4%BA%8B%E5%8A%A1/"/>
    <category term="存储过程" scheme="https://threcial.github.io/tags/%E5%AD%98%E5%82%A8%E8%BF%87%E7%A8%8B/"/>
    <category term="视图" scheme="https://threcial.github.io/tags/%E8%A7%86%E5%9B%BE/"/>
    <content>
      <![CDATA[<p>**事务：**保证一组数据修改要么都成功，要么都失败 <strong>视图</strong>：把复杂查询封装成一张虚拟表<br><strong>存储过程&#x2F;函数</strong>：把 SQL 逻辑封装在数据库里</p><h2 id="事务：把一组修改当成一个整体"><a href="#事务：把一组修改当成一个整体" class="headerlink" title="事务：把一组修改当成一个整体"></a>事务：把一组修改当成一个整体</h2><p>事务最容易理解的场景就是转账，比如李四给张三转 500 元，表面上是两条 <code>UPDATE</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">UPDATE account SET money = money - 500 WHERE name = &#x27;李四&#x27;;</span><br><span class="line">UPDATE account SET money = money + 500 WHERE name = &#x27;张三&#x27;;</span><br></pre></td></tr></table></figure><p>如果第一条执行成功，第二条还没执行时程序崩了，就会出现李四少了 500，张三没收到 500 的情况</p><p>事务要解决的就是这种“中间状态不能留下来”的问题</p><p>不加事务时，第二步出错会留下错误数据，加事务后，异常时可以回滚</p><p>事务写法：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">BEGIN;</span><br><span class="line">UPDATE account SET money = money - 500 WHERE name = &#x27;李四&#x27;;</span><br><span class="line">UPDATE account SET money = money + 500WHERE name = &#x27;张三&#x27;;</span><br><span class="line">COMMIT;</span><br></pre></td></tr></table></figure><p>如果发现了异常就 <code>ROLLBACK;</code></p><p>常见语法：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">START TRANSACTION;   或者  BEGIN;              开启一个事务</span><br><span class="line">COMMIT;                                         提交事务</span><br><span class="line">ROLLBACK;                                       回滚</span><br></pre></td></tr></table></figure><p><strong><code>COMMIT</code> 之后就不能再 <code>ROLLBACK</code>，回滚只能撤销还没有提交的事务</strong></p><p>MySQL 默认自动提交每句 SQL 语句</p><p>查看自动提交状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">SELECT @@autocommit;</span><br><span class="line"></span><br><span class="line">1：自动提交</span><br><span class="line">0：手动提交</span><br></pre></td></tr></table></figure><p>事务的四个特征 <strong>ACID</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Atomicity      原子性：事务不可再分，要么全部成功，要么全部失败</span><br><span class="line">Consistency    一致性：事务执行前后，数据要保持合理状态</span><br><span class="line">Isolation      隔离性：多个事务之间互相影响的程度</span><br><span class="line">Durability     持久性：事务提交后，数据修改要能持久保存</span><br></pre></td></tr></table></figure><h2 id="视图：保存查询逻辑"><a href="#视图：保存查询逻辑" class="headerlink" title="视图：保存查询逻辑"></a>视图：保存查询逻辑</h2><p>视图可以理解为一张虚拟表，它本身不保存数据，保存的是一条查询语句。查询视图时，MySQL 会根据视图定义去底层表里动态取数据</p><p>比如有一张学生表，创建一个只看部分字段的视图：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CREATE VIEW student_simple AS SELECT id, name FROM student;</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM student_simple;</span><br><span class="line">看起来像查表，实际是查这个视图背后的 SELECT id, name FROM student</span><br></pre></td></tr></table></figure><h3 id="视图的作用"><a href="#视图的作用" class="headerlink" title="视图的作用"></a>视图的作用</h3><p>第一，简化复杂查询。多表连接、过滤、计算字段可以封装成视图，以后直接查视图</p><p>第二，控制访问权限。比如底层表有工资、身份证、手机号，但只想让某个用户看姓名和部门，就可以只授权访问视图</p><p>第三，屏蔽部分表结构变化。如果底层表增加了视图没用到的字段，访问视图的人一般不受影响</p><h3 id="操作视图"><a href="#操作视图" class="headerlink" title="操作视图"></a>操作视图</h3><p>语法：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CREATE OR REPLACE VIEW 视图名 AS SELECT语句 WITH CASCADED CHECK OPTION;</span><br></pre></td></tr></table></figure><p>删除视图：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">DROP VIEW IF EXISTS 视图名;</span><br></pre></td></tr></table></figure><h2 id="存储过程：把-SQL-逻辑封装在数据库里"><a href="#存储过程：把-SQL-逻辑封装在数据库里" class="headerlink" title="存储过程：把 SQL 逻辑封装在数据库里"></a>存储过程：把 SQL 逻辑封装在数据库里</h2><p>存储过程就是保存在数据库中的一段 SQL 语句集合。它的意义是把一组重复使用的 SQL 逻辑封装起来，需要时 <code>CALL</code> 一下</p><p>一个简单的存储过程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">DELIMITER $$</span><br><span class="line"></span><br><span class="line">CREATE PROCEDURE p1()</span><br><span class="line">BEGIN</span><br><span class="line">    SELECT COUNT(*) FROM student;</span><br><span class="line">END$$</span><br><span class="line"></span><br><span class="line">DELIMITER ;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CALL p1();</span><br></pre></td></tr></table></figure><p><strong>因为存储过程内部有很多分号，如果仍然用 <code>;</code> 作为结束符，MySQL 命令行会提前认为语句结束，所以要先临时把结束符改成 <code>$$</code>，过程写完后再改回来</strong></p><p>删除：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">DROP PROCEDURE IF EXISTS p1;</span><br></pre></td></tr></table></figure><h3 id="变量：系统变量、用户变量、局部变量"><a href="#变量：系统变量、用户变量、局部变量" class="headerlink" title="变量：系统变量、用户变量、局部变量"></a>变量：系统变量、用户变量、局部变量</h3><p>系统变量是 MySQL 服务器层面的变量，分全局和会话</p><p>查看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">SHOW GLOBAL VARIABLES;</span><br><span class="line">SHOW SESSION VARIABLES;</span><br><span class="line">SHOW VARIABLES LIKE &#x27;innodb%&#x27;;</span><br><span class="line">ELECT @@global.max_connections;</span><br><span class="line">SELECT @@session.autocommit;</span><br></pre></td></tr></table></figure><p>设置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SET GLOBAL max_connections = 500;</span><br><span class="line">SET SESSION autocommit = 0;</span><br></pre></td></tr></table></figure><p>用户变量是当前连接里的变量，不用提前声明，前面加 <code>@</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SET @num = 100;</span><br><span class="line">SELECT @num;</span><br></pre></td></tr></table></figure><p>也可以把查询结果放进去：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SELECT COUNT(*) INTO @num FROM student;</span><br><span class="line">SELECT @num;</span><br></pre></td></tr></table></figure><p>局部变量只能在 <code>BEGIN ... END</code> 块里用，需要 <code>DECLARE</code> 声明：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">DECLARE total INT DEFAULT 0;</span><br><span class="line">SET total = 100;</span><br><span class="line">SELECT total;</span><br></pre></td></tr></table></figure><h3 id="参数：IN、OUT、INOUT"><a href="#参数：IN、OUT、INOUT" class="headerlink" title="参数：IN、OUT、INOUT"></a>参数：<code>IN</code>、<code>OUT</code>、<code>INOUT</code></h3><p>存储过程参数有三种：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">IN：输入参数，调用时传入，默认类型</span><br><span class="line">OUT：输出参数，调用后作为返回值</span><br><span class="line">INOUT：既能传入，也能被过程修改后带出</span><br></pre></td></tr></table></figure><p>比如判断成绩：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">DELIMITER $$</span><br><span class="line">CREATE PROCEDURE p_score(IN score INT, OUT result VARCHAR(10))</span><br><span class="line">BEGIN</span><br><span class="line">    IF score &gt;= 85 THEN</span><br><span class="line">        SET result = &#x27;优秀&#x27;;</span><br><span class="line">    ELSEIF score &gt;= 60 THEN</span><br><span class="line">        SET result = &#x27;及格&#x27;;</span><br><span class="line">    ELSE</span><br><span class="line">        SET result = &#x27;不及格&#x27;;</span><br><span class="line">    END IF;</span><br><span class="line">END$$</span><br><span class="line">DELIMITER ;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CALL p_score(58, @result);</span><br><span class="line">SELECT @result;</span><br></pre></td></tr></table></figure><h3 id="控制语句：IF、循环"><a href="#控制语句：IF、循环" class="headerlink" title="控制语句：IF、循环"></a>控制语句：<code>IF</code>、循环</h3><p><code>IF</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">IF 条件 THEN</span><br><span class="line">    -- SQL</span><br><span class="line">ELSEIF 条件 THEN</span><br><span class="line">    -- SQL</span><br><span class="line">ELSE</span><br><span class="line">    -- SQL</span><br><span class="line">END IF;</span><br></pre></td></tr></table></figure><p><code>WHILE</code> 是先判断再执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">DELIMITER $$</span><br><span class="line"></span><br><span class="line">CREATE PROCEDURE p_sum(IN n INT)</span><br><span class="line">BEGIN</span><br><span class="line">    DECLARE total INT DEFAULT 0;</span><br><span class="line"></span><br><span class="line">    WHILE n &gt; 0 DO</span><br><span class="line">        SET total = total + n;</span><br><span class="line">        SET n = n - 1;</span><br><span class="line">    END WHILE;</span><br><span class="line"></span><br><span class="line">    SELECT total;</span><br><span class="line">END$$</span><br><span class="line"></span><br><span class="line">DELIMITER ;</span><br></pre></td></tr></table></figure><p><code>REPEAT</code> 是先执行一次，再判断退出条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">DELIMITER $$</span><br><span class="line">CREATE PROCEDURE p_repeat(IN n INT)</span><br><span class="line">BEGIN</span><br><span class="line">    DECLARE total INT DEFAULT 0;</span><br><span class="line">    REPEAT</span><br><span class="line">        SET total = total + n;</span><br><span class="line">        SET n = n - 1;</span><br><span class="line">    UNTIL n &lt;= 0</span><br><span class="line">    END REPEAT;</span><br><span class="line">    SELECT total;</span><br><span class="line">END$$</span><br><span class="line">DELIMITER ;</span><br></pre></td></tr></table></figure><p><code>LOOP</code> 本身没有条件，需要配合 <code>LEAVE</code> 退出，或者用 <code>ITERATE</code> 跳过当前循环剩余内容进入下一轮：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">DELIMITER $$</span><br><span class="line">CREATE PROCEDURE p_loop(IN n INT)</span><br><span class="line">BEGIN</span><br><span class="line">    DECLARE total INT DEFAULT 0;</span><br><span class="line">    sum_loop: LOOP</span><br><span class="line">        IF n &lt;= 0 THEN</span><br><span class="line">            LEAVE sum_loop;</span><br><span class="line">        END IF;</span><br><span class="line">        IF n % 2 = 1 THEN</span><br><span class="line">            SET n = n - 1;</span><br><span class="line">            ITERATE sum_loop;</span><br><span class="line">        END IF;</span><br><span class="line">        SET total = total + n;</span><br><span class="line">        SET n = n - 1;</span><br><span class="line">    END LOOP sum_loop;</span><br><span class="line">    SELECT total;</span><br><span class="line">END$$</span><br><span class="line">DELIMITER ;</span><br></pre></td></tr></table></figure><h3 id="游标：逐行处理查询结果"><a href="#游标：逐行处理查询结果" class="headerlink" title="游标：逐行处理查询结果"></a>游标：逐行处理查询结果</h3><p>游标用来保存查询结果集，然后一行一行取出来处理</p><p><strong>使用游标时，要先定义变量，再定义游标</strong>，游标和 handler 的声明顺序不对会报错</p><p>基本语法：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">DECLARE 游标名 CURSOR FOR 查询语句;</span><br><span class="line">OPEN 游标名;</span><br><span class="line">FETCH 游标名 INTO 变量1, 变量2;</span><br><span class="line">CLOSE 游标名;</span><br></pre></td></tr></table></figure><p><strong>注意：如果 <code>FETCH</code> 到最后没有数据时，会触发 <code>NOT FOUND</code></strong></p><p>所以应该配合条件处理程序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">DELIMITER $$</span><br><span class="line">CREATE PROCEDURE p_cursor(IN uage INT)</span><br><span class="line">BEGIN</span><br><span class="line">    DECLARE uname VARCHAR(100);</span><br><span class="line">    DECLARE uid INT;</span><br><span class="line">    DECLARE done INT DEFAULT 0;</span><br><span class="line">    DECLARE u_cursor CURSOR FOR</span><br><span class="line">        SELECT ename, id</span><br><span class="line">        FROM emp</span><br><span class="line">        WHERE id &lt;= uage;</span><br><span class="line">    DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = 1;</span><br><span class="line">    DROP TABLE IF EXISTS tb_user_pro;</span><br><span class="line">    CREATE TABLE IF NOT EXISTS tb_user_pro(</span><br><span class="line">        id INT PRIMARY KEY AUTO_INCREMENT,</span><br><span class="line">        name VARCHAR(100),</span><br><span class="line">        uid INT</span><br><span class="line">    );</span><br><span class="line">    OPEN u_cursor;</span><br><span class="line">    read_loop: LOOP</span><br><span class="line">        FETCH u_cursor INTO uname, uid;</span><br><span class="line">        IF done = 1 THEN</span><br><span class="line">            LEAVE read_loop;</span><br><span class="line">        END IF;</span><br><span class="line">        INSERT INTO tb_user_pro(name, uid)</span><br><span class="line">        VALUES (uname, uid);</span><br><span class="line">    END LOOP read_loop;</span><br><span class="line">    CLOSE u_cursor;</span><br><span class="line">END$$</span><br><span class="line">DELIMITER ;</span><br></pre></td></tr></table></figure><h3 id="条件处理程序-Handler"><a href="#条件处理程序-Handler" class="headerlink" title="条件处理程序 Handler"></a>条件处理程序 Handler</h3><p>条件处理程序用于定义遇到错误或特定状态时怎么处理，比如使用游标的时候：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = 1;</span><br></pre></td></tr></table></figure><h3 id="存储函数"><a href="#存储函数" class="headerlink" title="存储函数"></a>存储函数</h3><p>存储函数可以理解成有返回值的存储过程。它必须通过 <code>RETURNS</code> 指定返回类型，用 <code>RETURN</code> 返回值</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">DELIMITER $$</span><br><span class="line"></span><br><span class="line">CREATE FUNCTION fun1(n INT)</span><br><span class="line">RETURNS INT</span><br><span class="line">DETERMINISTIC</span><br><span class="line">BEGIN</span><br><span class="line">    DECLARE total INT DEFAULT 0;</span><br><span class="line"></span><br><span class="line">    WHILE n &gt; 0 DO</span><br><span class="line">        SET total = total + n;</span><br><span class="line">        SET n = n - 1;</span><br><span class="line">    END WHILE;</span><br><span class="line"></span><br><span class="line">    RETURN total;</span><br><span class="line">END$$</span><br><span class="line"></span><br><span class="line">DELIMITER ;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT fun1(100);</span><br></pre></td></tr></table></figure><p>存储函数的参数只能是输入类型，本质上通过返回值输出结果</p>]]>
    </content>
    <id>https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/mysql-%E4%BA%8B%E5%8A%A1%E3%80%81%E8%A7%86%E5%9B%BE%E3%80%81%E5%AD%98%E5%82%A8%E8%BF%87%E7%A8%8B/</id>
    <link href="https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/mysql-%E4%BA%8B%E5%8A%A1%E3%80%81%E8%A7%86%E5%9B%BE%E3%80%81%E5%AD%98%E5%82%A8%E8%BF%87%E7%A8%8B/"/>
    <published>2026-06-06T12:14:32.000Z</published>
    <summary>事务：保证一组数据修改要么都成功，要么都失败 视图：把复杂查询封装成一张虚拟表 存储过程/函数：把 SQL 逻辑封装在数据库里 事务：把一组修改当成一个整体 事务最容易理解的场景就是转账，比如李四给张</summary>
    <title>MySQL 事务、视图、存储过程</title>
    <updated>2026-06-06T12:14:32.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="数据库" scheme="https://threcial.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    <category term="sql" scheme="https://threcial.github.io/tags/sql/"/>
    <category term="查询语句" scheme="https://threcial.github.io/tags/%E6%9F%A5%E8%AF%A2%E8%AF%AD%E5%8F%A5/"/>
    <content>
      <![CDATA[<h2 id="管理数据变化"><a href="#管理数据变化" class="headerlink" title="管理数据变化"></a>管理数据变化</h2><p>数据变化主要是三件事：插入、修改、删除</p><p><strong>插入数据：<code>INSERT</code></strong></p><p>指定字段插入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">INSERT INTO stu(id, name) VALUES(1, &#x27;张三&#x27;);</span><br></pre></td></tr></table></figure><p>批量插入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">INSERT INTO stu(id, name, gender) VALUES</span><br><span class="line">(2, &#x27;李四1&#x27;, &#x27;男&#x27;),</span><br><span class="line">(3, &#x27;李四2&#x27;, &#x27;男&#x27;),</span><br><span class="line">(4, &#x27;李四3&#x27;, &#x27;男&#x27;);</span><br></pre></td></tr></table></figure><p>给所有字段插入时，可以省略字段列表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">INSERT INTO stu VALUES</span><br><span class="line">(2, &#x27;李四&#x27;, &#x27;男&#x27;, &#x27;1999-11-11&#x27;, 88.88, &#x27;lisi@example.com&#x27;, &#x27;13888888888&#x27;, 1);</span><br></pre></td></tr></table></figure><p>但实际使用时，推荐保留字段列表，因为表结构有可能发生改变，如果省略字段列表，一旦字段顺序或字段数量变化，旧 SQL 语句将失效</p><p><strong>修改数据：<code>UPDATE</code></strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">UPDATE 表名</span><br><span class="line">SET 字段1 = 值1,</span><br><span class="line">    字段2 = 值2</span><br><span class="line">WHERE 条件;</span><br></pre></td></tr></table></figure><p>比如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">UPDATE stu</span><br><span class="line">SET sex = &#x27;女&#x27;</span><br><span class="line">WHERE name = &#x27;张三&#x27;;</span><br></pre></td></tr></table></figure><p>一次改多个字段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">UPDATE stu</span><br><span class="line">SET birthday = &#x27;1999-12-12&#x27;,</span><br><span class="line">    score = 99.99</span><br><span class="line">WHERE name = &#x27;张三&#x27;;</span><br></pre></td></tr></table></figure><p><strong>如果没有 <code>WHERE</code>，整张表都会被改，所以真正操作数据时，要先用 <code>SELECT</code> 确认影响范围</strong></p><p><strong>删除数据：<code>DELETE</code> 和 <code>TRUNCATE</code></strong></p><p>删除指定记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">DELETE FROM stu WHERE name = &#x27;张三&#x27;;</span><br></pre></td></tr></table></figure><p>删除全表数据：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">DELETE FROM stu;</span><br></pre></td></tr></table></figure><p>清空表也可以用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TRUNCATE TABLE stu;</span><br></pre></td></tr></table></figure><p>清空大表时，<code>TRUNCATE</code> 更快，它的效果类似重置整张表，不能像普通 <code>DELETE WHERE</code> 那样精确控制范围</p><p><strong>删之前先查询，确定范围</strong></p><h2 id="管理数据查询"><a href="#管理数据查询" class="headerlink" title="管理数据查询"></a>管理数据查询</h2><p>一个完整的查询骨架可以记成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">SELECT</span><br><span class="line">    字段列表</span><br><span class="line">FROM</span><br><span class="line">   表名列表</span><br><span class="line">WHERE</span><br><span class="line">    条件列表</span><br><span class="line">GROUP BY</span><br><span class="line">    分组字段</span><br><span class="line">HAVING</span><br><span class="line">    分组后条件</span><br><span class="line">ORDER BY</span><br><span class="line">    排序字段</span><br><span class="line">LIMIT</span><br><span class="line">    分页限定;</span><br></pre></td></tr></table></figure><h3 id="基础查询"><a href="#基础查询" class="headerlink" title="基础查询"></a>基础查询</h3><p>查询全部字段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM stu;</span><br></pre></td></tr></table></figure><p>查询指定字段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT name, age FROM stu;</span><br></pre></td></tr></table></figure><p>去重：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT DISTINCT address FROM stu;</span><br></pre></td></tr></table></figure><p>别名：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SELECT name, math AS 数学成绩, english AS 英语成绩 FROM stu;</span><br><span class="line">SELECT name, math 数学成绩, english 英语成绩 FROM stu;        as可以省略</span><br></pre></td></tr></table></figure><h3 id="条件查询：WHERE"><a href="#条件查询：WHERE" class="headerlink" title="条件查询：WHERE"></a>条件查询：<code>WHERE</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM stu WHERE age &gt; 20;                      大于</span><br><span class="line">SELECT * FROM stu WHERE age &gt;= 20;                     大于等于</span><br><span class="line">SELECT * FROM stu WHERE age &gt; 20 AND age  18;                   不等于的两种写法</span><br><span class="line">SELECT * FROM stu WHERE age IN (18, 20, 22);         多个取值</span><br><span class="line">SELECT * FROM stu WHERE age NOT IN (55, 45, 57);     排除多个取值</span><br></pre></td></tr></table></figure><p><strong><code>NULL</code> 不能用 <code>=</code> 或 <code>!=</code> 判断：</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM stu WHERE english IS NULL;</span><br><span class="line">SELECT * FROM stu WHERE english IS NOT NULL;</span><br></pre></td></tr></table></figure><h3 id="模糊查询：LIKE"><a href="#模糊查询：LIKE" class="headerlink" title="模糊查询：LIKE"></a>模糊查询：<code>LIKE</code></h3><p>通配符：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">_ ：单个任意字符</span><br><span class="line">% ：任意多个字符</span><br></pre></td></tr></table></figure><p>用法：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM stu WHERE name LIKE &#x27;马%&#x27;;             马字开头</span><br><span class="line">SELECT * FROM stu WHERE name LIKE &#x27;_花%&#x27;;            二个字是花</span><br><span class="line">SELECT * FROM stu WHERE name LIKE &#x27;%德%&#x27;;            名字中包含德</span><br></pre></td></tr></table></figure><h3 id="排序：ORDER-BY"><a href="#排序：ORDER-BY" class="headerlink" title="排序：ORDER BY"></a>排序：<code>ORDER BY</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM stu ORDER BY age;         按年龄升序，默认就是升序</span><br><span class="line">SELECT * FROM stu ORDER BY age ASC;      完整写法</span><br><span class="line">SELECT * FROM stu ORDER BY age DESC;      降序</span><br></pre></td></tr></table></figure><p>多个排序条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM stu ORDER BY math DESC, english ASC;</span><br></pre></td></tr></table></figure><p>多个排序条件的规则是：先按第一个字段排序，第一个字段相同，再按第二个字段排序</p><h3 id="聚合函数：纵向计算"><a href="#聚合函数：纵向计算" class="headerlink" title="聚合函数：纵向计算"></a>聚合函数：纵向计算</h3><p>聚合函数是对一列数据做整体计算</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">COUNT：统计数量</span><br><span class="line">MAX：最大值</span><br><span class="line">MIN：最小值</span><br><span class="line">SUM：求和</span><br><span class="line">AVG：平均值</span><br></pre></td></tr></table></figure><p>示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">SELECT COUNT(*) FROM stu;</span><br><span class="line">SELECT MAX(math) FROM stu;</span><br><span class="line">SELECT MIN(math) FROM stu;</span><br><span class="line">SELECT SUM(math) FROM stu;</span><br><span class="line">SELECT AVG(math) FROM stu;</span><br></pre></td></tr></table></figure><p><code>NULL</code> 不会参与聚合运算</p><p>统计行数：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SELECT COUNT(*) FROM stu;</span><br><span class="line">SELECT COUNT(id) FROM stu;</span><br></pre></td></tr></table></figure><h3 id="分组查询：GROUP-BY-和-HAVING"><a href="#分组查询：GROUP-BY-和-HAVING" class="headerlink" title="分组查询：GROUP BY 和 HAVING"></a>分组查询：<code>GROUP BY</code> 和 <code>HAVING</code></h3><p>按性别分组，统计数学平均分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT sex, AVG(math) FROM stu GROUP BY sex;</span><br></pre></td></tr></table></figure><p>先过滤，再分组：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT sex, AVG(math), COUNT(*) FROM stu WHERE math &gt; 70 GROUP BY sex;</span><br></pre></td></tr></table></figure><p>分组后再过滤：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT sex, AVG(math), COUNT(*) FROM stu WHERE math &gt; 70 GROUP BY sex HAVING COUNT(*) &gt; 2;</span><br></pre></td></tr></table></figure><p><strong>WHERE：分组前过滤，不能直接判断聚合函数<br>HAVING：分组后过滤，可以判断聚合函数</strong></p><h3 id="分页查询：LIMIT"><a href="#分页查询：LIMIT" class="headerlink" title="分页查询：LIMIT"></a>分页查询：<code>LIMIT</code></h3><p>基本格式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT 字段列表 FROM 表名 LIMIT 起始索引, 查询条目数;</span><br></pre></td></tr></table></figure><p>起始索引从 <code>0</code> 开始</p><p>查询前 3 条：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM stu LIMIT 0, 3;</span><br></pre></td></tr></table></figure><p>从第 4 条开始，查 10 条：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM stu LIMIT 3, 10;</span><br></pre></td></tr></table></figure><h3 id="多表查询"><a href="#多表查询" class="headerlink" title="多表查询"></a>多表查询</h3><p>两张表直接联查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM emp, dept;</span><br></pre></td></tr></table></figure><p>会得到笛卡尔积，比如 6 个员工、4 个部门，结果就是 24 行，其中大量是无意义组合</p><p>所以使用多表查询必须添加关联条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM emp, dept WHERE emp.dep_id = dept.id;</span><br></pre></td></tr></table></figure><p>写多表查询的关键是：<strong>表和表靠哪个字段关联？要保留匹配数据，还是保留某一边的全部数据？</strong></p><p>内连接：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM emp INNER JOIN dept ON emp.dep_id = dept.did;</span><br></pre></td></tr></table></figure><p>只保留两边都匹配的数据</p><p>外连接：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM emp LEFT JOIN dept ON emp.dep_id = dept.did;</span><br></pre></td></tr></table></figure><p>保留左表 emp 的全部数据，右表 dept 能匹配就显示，不能匹配就显示 NULL</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM emp RIGHT JOIN dept ON emp.dep_id = dept.did;</span><br></pre></td></tr></table></figure><p>保留右表 dept 的全部数据，左表 emp 能匹配就显示，不能匹配就显示 NULL</p><h3 id="子查询：把查询结果当条件或临时表"><a href="#子查询：把查询结果当条件或临时表" class="headerlink" title="子查询：把查询结果当条件或临时表"></a>子查询：把查询结果当条件或临时表</h3><p>子查询按结果形态分几种</p><p>单行单列：当普通值用</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM emp WHERE salary = </span><br><span class="line">( SELECT salary FROM emp WHERE name = &#x27;猪八戒&#x27;);</span><br></pre></td></tr></table></figure><p>多行单列：配合 <code>IN</code> 或 <code>NOT IN</code></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM emp WHERE dep_id IN </span><br><span class="line">( SELECT id FROM dept WHERE dname = &#x27;财务部&#x27; OR dname = &#x27;市场部&#x27;);</span><br></pre></td></tr></table></figure><p>多行多列：当临时表用</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT * FROM ( SELECT * FROM emp  WHERE join_date &gt; &#x27;2011-11-11&#x27;) t1, dept WHERE t1.dep_id = dept.did;</span><br></pre></td></tr></table></figure><p>这里的 <code>t1</code> 是派生表别名，不能省略</p>]]>
    </content>
    <id>https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/%E5%9F%BA%E7%A1%80sql%E8%AF%AD%E5%8F%A5-%E4%B8%8B/</id>
    <link href="https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/%E5%9F%BA%E7%A1%80sql%E8%AF%AD%E5%8F%A5-%E4%B8%8B/"/>
    <published>2026-06-06T11:35:25.000Z</published>
    <summary>管理数据变化 数据变化主要是三件事：插入、修改、删除 插入数据：INSERT 指定字段插入： INSERT INTO stu(id, name) VALUES(1, '张三'); 批量插入： INSE</summary>
    <title>基础SQL语句 下</title>
    <updated>2026-06-06T11:35:25.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="数据库" scheme="https://threcial.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    <category term="mysql" scheme="https://threcial.github.io/tags/mysql/"/>
    <category term="语法" scheme="https://threcial.github.io/tags/%E8%AF%AD%E6%B3%95/"/>
    <content>
      <![CDATA[<h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>SQL 全称是 Structured Query Language，结构化查询语言，SQL 大致可以分成几类：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">DDL：Data Definition Language    数据定义语言</span><br><span class="line">DML：Data Manipulation Language  数据操作语言</span><br><span class="line">DQL：Data Query Language         数据查询语言</span><br><span class="line">DCL：Data Control Language       数据控制语言</span><br></pre></td></tr></table></figure><p>因为大部分时候在操作表，为了便于记忆，可以不严谨地分为下面三种语句</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">管理表结构或库结构</span><br><span class="line">管理表数据变化</span><br><span class="line">管理表数据查询          </span><br></pre></td></tr></table></figure><p>SQL 可以单行写，也可以多行写，但一条语句结束要加分号。MySQL 关键字不区分大小写</p><p>注释有三种常见写法：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">--           单行注释，注意 -- 后面要有空格</span><br><span class="line">#            MySQL 特有的单行注释</span><br><span class="line">/*    */     多行注释</span><br></pre></td></tr></table></figure><h2 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h2><p>MySQL 中储存的数据有以下数据类型</p><ul><li><strong>tinyint</strong><br>1字节有符号整型，范围是-128-127</li><li><strong>tinyint unsigned</strong><br>1字节无符号整型，范围是0-255</li><li><strong>int</strong><br>4字节有符号整型，范围是-2147483648-2147483647</li><li><strong>int unsigned</strong><br>4字节无符号整型，范围是0-4294967295</li><li><strong>bigint</strong><br>8字节有符号整型，范围是-9223372036854775808-9223372036854775807</li><li><strong>bigint unsigned</strong><br>8字节无符号整型，范围是0-18446744073709551615</li><li><strong>float</strong><br>小数类型，4字节,不精准计算可以使用，不推荐使用</li><li><strong>double</strong><br>小数类型，8字节,不精准计算可以使用，不推荐使用</li><li><strong>decimal</strong><br>小数类型，精准计算使用，decimal(m,d),m表示数字总个数，d是小数点后的个数，m的最大值是65,d最大值是30，如果小数部分超过了限制的长度，会进行四舍五入</li><li><strong>char</strong><br>固定长度字符串类型，最大255个字符</li><li><strong>varchar</strong><br>可变长度字符串类型，最大21000个字符</li><li><strong>text</strong><br>文本类型，最大65535个字符，使用的时候不需要指定字符串大小</li><li><strong>datetime</strong><br>时间类型，保存的格式是年月日时分秒，存储时间与时区无关</li><li><strong>date</strong><br>时间类型，保存的格式是年月日，存储时间与时区无关</li><li><strong>timestamp</strong><br>时间戳类型，保存的是从1970-01-01 00:00:00到现在的时间戳，当将TIMESTAMP值插入到表中时，MySQL会将其从连接的时区转换为UTC后进行存储。当查询TIMESTAMP值时，MySQL会将UTC值转换回连接的时区进行显示，存储时间与时区有关，最大范围支持到2038-01-19 03:14:07</li></ul><h2 id="环境查看"><a href="#环境查看" class="headerlink" title="环境查看"></a>环境查看</h2><p>使用 MySQL 时，应该先看当前环境</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">SHOW DATABASES;           查看所有数据库</span><br><span class="line">USE 数据库名;              使用数据库</span><br><span class="line">SELECT DATABASE();        查看当前使用的数据库</span><br><span class="line">SHOW TABLES;              查看当前数据库内所有表</span><br><span class="line">DESC 表名;                 查看表结构</span><br><span class="line"></span><br><span class="line">数据库在使用时一般以库为单位，操作库里的表，所以需要使用 use</span><br></pre></td></tr></table></figure><p>命令行里还有一个显示方式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SHOW CREATE TABLE mytable\G;</span><br><span class="line">\G 不是 SQL 语法本身，而是 MySQL 客户端的纵向显示方式，一般用于字段多的时候</span><br></pre></td></tr></table></figure><h2 id="管理结构"><a href="#管理结构" class="headerlink" title="管理结构"></a>管理结构</h2><p>库</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CREATE DATABASE IF NOT EXISTS db_test;    创建库db_test</span><br><span class="line">DROP DATABASE IF EXISTS db_test;          删除库db_test</span><br></pre></td></tr></table></figure><p>表</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE IF NOT EXISTS 表名 (                             创建表</span><br><span class="line">    字段名 数据类型 约束,</span><br><span class="line">    字段名 数据类型 约束</span><br><span class="line">);</span><br><span class="line">DROP TABLE IF EXISTS student;                    删除表</span><br><span class="line"></span><br><span class="line">ALTER TABLE stu ADD address VARCHAR(50);         添加字段</span><br><span class="line">ALTER TABLE stu MODIFY address CHAR(50);         修改字段类型</span><br><span class="line">ALTER TABLE stu CHANGE address addr VARCHAR(50); 修改字段名和字段类型</span><br><span class="line">ALTER TABLE stu DROP addr;                        删除字段</span><br><span class="line">ALTER TABLE student RENAME TO stu;                修改表名</span><br><span class="line">            MODIFY：只改字段定义，不改字段名</span><br><span class="line">            CHANGE：字段名和字段定义一起改</span><br></pre></td></tr></table></figure><h3 id="约束"><a href="#约束" class="headerlink" title="约束"></a>约束</h3><p>约束是表结构里很关键的一部分，把规则放到数据库层面可以避免错误数据被写进去</p><p><strong>默认约束：<code>DEFAULT</code></strong>，未指定值时自动使用默认值</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE test (</span><br><span class="line">    id INT PRIMARY KEY AUTO_INCREMENT,</span><br><span class="line">    bonus INT DEFAULT 0</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>建表后添加默认值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test MODIFY bonus INT DEFAULT 0;</span><br></pre></td></tr></table></figure><p>删除默认值本质是重新定义字段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test MODIFY bonus INT;</span><br></pre></td></tr></table></figure><p><strong>非空约束：<code>NOT NULL</code></strong>，字段必须有值</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE test (</span><br><span class="line">    id INT PRIMARY KEY AUTO_INCREMENT,</span><br><span class="line">    name VARCHAR(50) NOT NULL</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>建表后添加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test MODIFY name VARCHAR(50) NOT NULL;</span><br></pre></td></tr></table></figure><p>删除非空约束本质也是重新修改字段定义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test MODIFY name VARCHAR(50);</span><br></pre></td></tr></table></figure><p><strong>唯一约束：<code>UNIQUE</code></strong>，字段不能重复</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE test (</span><br><span class="line">    id INT PRIMARY KEY AUTO_INCREMENT,</span><br><span class="line">    name VARCHAR(50) UNIQUE</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>给约束命名：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE test (</span><br><span class="line">    id INT PRIMARY KEY AUTO_INCREMENT,</span><br><span class="line">    name VARCHAR(50),</span><br><span class="line">    CONSTRAINT uk_name UNIQUE(name)</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>建表后添加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test ADD CONSTRAINT uk_name UNIQUE(name);</span><br></pre></td></tr></table></figure><p>删除唯一约束时用的是 <code>DROP INDEX</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test DROP INDEX uk_name;</span><br></pre></td></tr></table></figure><p><strong>MySQL 里唯一约束底层依赖唯一索引，所以删除是 <code>DROP INDEX</code></strong></p><p><strong>主键约束：<code>PRIMARY KEY</code></strong>，主键是一行数据的唯一标识，特点是：非空、唯一、一张表只能有一个主键</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE test (</span><br><span class="line">    id INT PRIMARY KEY AUTO_INCREMENT,</span><br><span class="line">    name VARCHAR(50)</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE test (</span><br><span class="line">    id INT,</span><br><span class="line">    name VARCHAR(50),</span><br><span class="line">    CONSTRAINT uk_name PRIMARY KEY(id)</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>建表后添加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test ADD PRIMARY KEY(id);</span><br></pre></td></tr></table></figure><p>删除主键：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test DROP PRIMARY KEY;</span><br></pre></td></tr></table></figure><p>主键通常使用自增 ID</p><p><strong>外键约束：<code>FOREIGN KEY</code></strong>，外键用于建立表和表之间的数据库层面的关系</p><p>比如员工属于部门，员工表里的部门 <code>id</code> 指向部门表的 <code>id</code></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE dept(</span><br><span class="line">    id INT PRIMARY KEY AUTO_INCREMENT,</span><br><span class="line">    name VARCHAR(20),</span><br><span class="line">    addr VARCHAR(20)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line">CREATE TABLE emp(</span><br><span class="line">    id INT PRIMARY KEY AUTO_INCREMENT,</span><br><span class="line">    name VARCHAR(20),</span><br><span class="line">    age INT,</span><br><span class="line">    dep_id INT,</span><br><span class="line">    CONSTRAINT fk_emp_dept FOREIGN KEY(dep_id) REFERENCES dept(id)</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>建表后添加外键：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test ADD CONSTRAINT uk_name FOREIGN KEY(id) REFERENCES test2(id);</span><br></pre></td></tr></table></figure><p>删除外键：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER TABLE test DROP FOREIGN KEY uk_name;</span><br></pre></td></tr></table></figure><p>外键的意义是：如果部门表里 <code>id=1</code> 的部门还有员工引用，那么直接删除这个部门会失败。它保证的是引用完整性。</p><p><strong>外键字段和被引用字段的数据类型要一致<br>被引用字段通常必须是主键或唯一键<br>两张表要使用支持外键的存储引擎，比如 InnoDB<br>删除外键用 DROP FOREIGN KEY，不是 DROP INDEX</strong></p>]]>
    </content>
    <id>https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/%E5%9F%BA%E7%A1%80sql%E8%AF%AD%E5%8F%A5-%E4%B8%8A/</id>
    <link href="https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/%E5%9F%BA%E7%A1%80sql%E8%AF%AD%E5%8F%A5-%E4%B8%8A/"/>
    <published>2026-06-05T12:46:01.000Z</published>
    <summary>概念 SQL 全称是 Structured Query Language，结构化查询语言，SQL 大致可以分成几类： DDL：Data Definition Language 数据定义语言 DML：D</summary>
    <title>基础SQL语句 上</title>
    <updated>2026-06-05T12:46:01.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="数据库" scheme="https://threcial.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    <category term="mysql" scheme="https://threcial.github.io/tags/mysql/"/>
    <category term="数据库" scheme="https://threcial.github.io/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    <content>
      <![CDATA[<h2 id="数据库概念"><a href="#数据库概念" class="headerlink" title="数据库概念"></a>数据库概念</h2><p>数据库，英文是 Database，简称 DB，可以把他简单理解为<strong>有组织地存储和管理数据的仓库</strong>，普通文件也能存储数据，但数据库会按照库、表、字段、记录这些结构来管理数据</p><p>数据库管理系统，英文是 DBMS，全称 Database Management System，是实际安装在系统里的大型软件，MySQL、MariaDB、Oracle、SQL Server、PostgreSQL、DB2、SQLite 这些都属于数据库管理系统</p><p>具体概念如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">DB：数据库，保存数据的仓库</span><br><span class="line">DBMS：数据库管理系统，管理数据库的软件</span><br><span class="line">SQL：操作关系型数据库的语言</span><br><span class="line">MySQL：一种具体的数据库管理系统</span><br></pre></td></tr></table></figure><h2 id="MySQL数据库配置"><a href="#MySQL数据库配置" class="headerlink" title="MySQL数据库配置"></a>MySQL数据库配置</h2><p>MySQL 常见主配置文件是 <code>/etc/my.cnf</code></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">max_connections=200                      设置最大连接数</span><br><span class="line">max_allowed_packet=32M</span><br><span class="line">character-set-server=utf8mb4             设置字符集</span><br><span class="line">collation-server=utf8mb4_unicode_ci      排序规则，决定字符串比较和排序方式</span><br><span class="line">skip_name_resolve                        跳过反向域名解析，可以加快连接速度</span><br><span class="line">skip-character-set-client-handshake      </span><br><span class="line"></span><br><span class="line">datadir=/data/mysql                       数据库目录</span><br><span class="line">socket=/var/lib/mysql/mysql.sock</span><br><span class="line"></span><br><span class="line">log-error=/var/log/mysqld.log             日志</span><br><span class="line">pid-file=/var/run/mysqld/mysqld.pid</span><br><span class="line"></span><br><span class="line">log_bin=/data/mysql_bin/mysql8            binlog的basename配置</span><br><span class="line">server_id=1                                 </span><br><span class="line">expire_logs_days=7                       保留七天</span><br><span class="line">max_binlog_size=100k                     大小设置</span><br><span class="line">sync_binlog=0                            写入硬盘频率，0为系统自动安排</span><br><span class="line"></span><br><span class="line">[client]</span><br><span class="line">default-character-set=utf8mb4           </span><br></pre></td></tr></table></figure><p><strong>给 MySQL 使用的目录需要注意权限问题，最好直接把目录的所有者和组都改成 mysql</strong></p><p>配置完成启动 mysqld 服务，在如下日志文件中会生成临时密码</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/var/log/mysqld.log</span><br></pre></td></tr></table></figure><p>找到临时密码以后登录</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql -u root -p</span><br></pre></td></tr></table></figure><p>远程登录则使用 -h 指定主机，-P 指定端口</p><p>登录后第一件事通常是修改 root 密码</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ALTER USER &#x27;root&#x27;@&#x27;localhost&#x27; IDENTIFIED BY &#x27;你的密码&#x27;;</span><br></pre></td></tr></table></figure><p>如果密码策略比较严格，简单密码会失败，可能要求包含大写、小写、数字、特殊字符，修改后刷新权限</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">FLUSH PRIVILEGES;</span><br></pre></td></tr></table></figure><p>检查配置文件是否生效</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">SHOW VARIABLES LIKE &#x27;%chara%&#x27;;</span><br><span class="line">SHOW MASTER STATUS;</span><br><span class="line">SHOW VARIABLES LIKE &#x27;%log_bin%&#x27;;</span><br><span class="line">SHOW VARIABLES LIKE &#x27;%datadir%&#x27;;</span><br><span class="line">SHOW VARIABLES LIKE &#x27;%coll%&#x27;;</span><br></pre></td></tr></table></figure><p>一些其他查看类语法</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">show processlist;            查看数据库正在执行的sql语句，不完整 </span><br><span class="line">show session status;         查看当前会话的数据库状态信息</span><br><span class="line">show global status;          查看整个数据库运行的状态信息</span><br><span class="line">show engine innodb status;   查看innodb引擎的状态信息</span><br><span class="line">set 参数 = 值                 临时修改mysql参数的值，重启后失效</span><br><span class="line">kill ID;                     杀掉某一个sql线程的命令，ID表示线程号</span><br></pre></td></tr></table></figure><h2 id="权限管理"><a href="#权限管理" class="headerlink" title="权限管理"></a>权限管理</h2><p>在 MySQL 中创建一个用户默认是最小权限，需要额外给予权限</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">CREATE USER &#x27;root&#x27;@&#x27;%&#x27; IDENTIFIED BY &#x27;某个密码&#x27;;</span><br><span class="line">GRANT ALL ON *.* TO &#x27;root&#x27;@&#x27;%&#x27; [ WITH GRANT OPTION ]; </span><br><span class="line">FLUSH PRIVILEGES;</span><br><span class="line"></span><br><span class="line">MySQL 中用户是由名字和ip共同构成，&#x27;%&#x27;在 MySQL 中表示任意数量字符，在这里代表任意地址</span><br><span class="line">授予权限时，all 表示所有权限，也可以根据具体权限划分。* 作用类似 shell 中的通配符，*.* 表示所有库中的所有表，实际使用时一定要遵循最小权限原则</span><br><span class="line"></span><br><span class="line">revoke select on data1.* from &#x27;alice&#x27;@&#x27;%&#x27;;</span><br><span class="line">收回权限：收回用户 &#x27;alice&#x27;@&#x27;%&#x27; 在 data1 库上任意表的 select 权限</span><br></pre></td></tr></table></figure><h2 id="忘记密码"><a href="#忘记密码" class="headerlink" title="忘记密码"></a>忘记密码</h2><p>如果忘记 root 密码，可以在配置文件中添加</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">skip-grant-tables</span><br></pre></td></tr></table></figure><p>来跳过密码验证，登录后修改 root 密码，同时记得再将配置文件改回去</p>]]>
    </content>
    <id>https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/%E6%95%B0%E6%8D%AE%E5%BA%93%E6%A6%82%E5%BF%B5%E4%BB%A5%E5%8F%8Amysql%E9%85%8D%E7%BD%AE/</id>
    <link href="https://threcial.github.io/%E6%95%B0%E6%8D%AE%E5%BA%93/%E6%95%B0%E6%8D%AE%E5%BA%93%E6%A6%82%E5%BF%B5%E4%BB%A5%E5%8F%8Amysql%E9%85%8D%E7%BD%AE/"/>
    <published>2026-06-04T05:30:03.000Z</published>
    <summary>数据库概念 数据库，英文是 Database，简称 DB，可以把他简单理解为有组织地存储和管理数据的仓库，普通文件也能存储数据，但数据库会按照库、表、字段、记录这些结构来管理数据 数据库管理系统，英文</summary>
    <title>数据库概念以及MySQL配置</title>
    <updated>2026-06-04T05:30:03.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="web服务" scheme="https://threcial.github.io/categories/web%E6%9C%8D%E5%8A%A1/"/>
    <category term="tomcat" scheme="https://threcial.github.io/tags/tomcat/"/>
    <content>
      <![CDATA[<p>Tomcat 是 Java 写的，运行离不开 JDK&#x2F;JRE，需要有 JAVA 环境</p><p>在 <code>catalina.sh</code> 和 <code>setclasspath.sh</code> 中添加</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">JAVA_HOME=java环境目录</span><br></pre></td></tr></table></figure><p>Tomcat 解压后常见目录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">bin       启动、关闭、核心脚本</span><br><span class="line">conf      配置文件</span><br><span class="line">lib       Tomcat 和 Web 应用依赖的公共库</span><br><span class="line">logs      日志</span><br><span class="line">webapps   应用发布目录</span><br><span class="line">temp      临时文件</span><br><span class="line">work      JSP 编译后的工作目录</span><br></pre></td></tr></table></figure><p><code>bin</code> 里常见：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">startup.sh    启动脚本</span><br><span class="line">shutdown.sh   关闭脚本</span><br><span class="line">catalina.sh   核心控制脚本</span><br></pre></td></tr></table></figure><p><code>conf</code> 里重点是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">server.xml          端口、Connector、Host 等核心配置</span><br><span class="line">web.xml             默认 Web 应用配置</span><br><span class="line">tomcat-users.xml    管理用户和角色</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/web%E6%9C%8D%E5%8A%A1/tomcat-%E5%9F%BA%E7%A1%80%E9%85%8D%E7%BD%AE/</id>
    <link href="https://threcial.github.io/web%E6%9C%8D%E5%8A%A1/tomcat-%E5%9F%BA%E7%A1%80%E9%85%8D%E7%BD%AE/"/>
    <published>2026-05-31T09:13:06.000Z</published>
    <summary>Tomcat 是 Java 写的，运行离不开 JDK/JRE，需要有 JAVA 环境 在 catalina.sh 和 setclasspath.sh 中添加 JAVA_HOME=java环境目录 To</summary>
    <title>tomcat 基础配置</title>
    <updated>2026-05-31T09:13:06.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="web服务" scheme="https://threcial.github.io/categories/web%E6%9C%8D%E5%8A%A1/"/>
    <category term="nginx" scheme="https://threcial.github.io/tags/nginx/"/>
    <category term="配置" scheme="https://threcial.github.io/tags/%E9%85%8D%E7%BD%AE/"/>
    <category term="性能优化" scheme="https://threcial.github.io/tags/%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/"/>
    <content>
      <![CDATA[<p>Nginx 默认参数偏保守，适合最小硬件环境。真实业务里通常会调整</p><h2 id="高并发优化：worker-和连接数"><a href="#高并发优化：worker-和连接数" class="headerlink" title="高并发优化：worker 和连接数"></a>高并发优化：worker 和连接数</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">worker_processes auto;</span><br><span class="line"></span><br><span class="line">events &#123;</span><br><span class="line">    worker_connections 4096;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>worker_processes</code> 通常按 CPU 核心数设置，直接 <code>auto</code> 就可以</p><p><code>worker_connections</code> 是每个 worker 的最大连接数，可以从 4096 起步，并且习惯上取 2 的指数。如果 Nginx 是反向代理，一个客户端请求可能同时占用客户端连接和后端连接，所以理论连接数不能简单等于客户端数量</p><h2 id="长连接优化：keepalive"><a href="#长连接优化：keepalive" class="headerlink" title="长连接优化：keepalive"></a>长连接优化：keepalive</h2><p>HTTP 基于 TCP，每次短连接都要三次握手、四次挥手。keepalive 的作用就是复用连接</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">keepalive_timeout 15;</span><br><span class="line">keepalive_requests 8192;</span><br></pre></td></tr></table></figure><p>默认是 65 秒，建议改到 15 秒左右，太长会让空闲连接长期占用 fd、worker_connections 和内存</p><p><code>keepalive_requests</code> 表示一个长连接最多处理多少个请求，达到后关闭</p><h2 id="数据传输优化：sendfile-和-gzip"><a href="#数据传输优化：sendfile-和-gzip" class="headerlink" title="数据传输优化：sendfile 和 gzip"></a>数据传输优化：sendfile 和 gzip</h2><p>静态文件传输建议开启：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sendfile on;</span><br><span class="line">这是零拷贝方向的优化，可以减少用户态和内核态之间的数据拷贝</span><br></pre></td></tr></table></figure><p>gzip 用于文本资源压缩：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">gzip on;</span><br><span class="line">gzip_proxied any;</span><br><span class="line">gzip_min_length 10k;</span><br><span class="line">gzip_comp_level 6;</span><br><span class="line">gzip_types text/plain application/x-javascript text/css application/xml text/javascript application/x-httpd-php application/javascript application/json;</span><br></pre></td></tr></table></figure><p>压缩能节省带宽，让用户更快拿到文本内容。但不要压缩图片、视频、音频、压缩包，这些本身已经压缩过，再压缩收益很小，还消耗 CPU</p><p>隐藏版本号：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">server_tokens off;</span><br></pre></td></tr></table></figure><h2 id="客户端缓存"><a href="#客户端缓存" class="headerlink" title="客户端缓存"></a>客户端缓存</h2><p>音视频、图片这类资源无法通过 gzip 得到明显收益，更适合让浏览器缓存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">location ~* \.(png|gif|jpg|mp3|mp4|wav|avi)$ &#123;</span><br><span class="line">    expires 1h;        可以写30m 1h 1d 表示30分钟、一小时、一天</span><br><span class="line">    root html;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>缓存时间要按业务来，频繁变化的资源不要长缓存，带 hash 或版本号的静态资源可以缓存更久</p><h2 id="负载均衡：Nginx-作为七层分发器"><a href="#负载均衡：Nginx-作为七层分发器" class="headerlink" title="负载均衡：Nginx 作为七层分发器"></a>负载均衡：Nginx 作为七层分发器</h2><p>单台服务器总会遇到瓶颈，也会有单点故障，所以需要在前面放分发器，也就是反向代理的实际应用</p><p>常见方式有</p><h3 id="轮询"><a href="#轮询" class="headerlink" title="轮询"></a>轮询</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">upstream web &#123;</span><br><span class="line">    server 192.168.0.120;</span><br><span class="line">    server 192.168.0.121;</span><br><span class="line">    server 192.168.0.122;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">server &#123;</span><br><span class="line">    listen 80;</span><br><span class="line"></span><br><span class="line">    location / &#123;</span><br><span class="line">        proxy_pass http://web;</span><br><span class="line">        proxy_set_header X-Real-IP $remote_addr;</span><br><span class="line">        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>默认按请求顺序轮询转发</p><h3 id="权重"><a href="#权重" class="headerlink" title="权重"></a>权重</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">upstream web &#123;</span><br><span class="line">    server 192.168.0.120 weight=10;</span><br><span class="line">    server 192.168.0.121 weight=3;</span><br><span class="line">    server 192.168.0.122;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>没有写 <code>weight</code> 时默认是 1，权重越高，分到的请求越多</p><h3 id="ip-hash"><a href="#ip-hash" class="headerlink" title="ip_hash"></a>ip_hash</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">upstream web &#123;</span><br><span class="line">    ip_hash;</span><br><span class="line">    server 192.168.0.120;</span><br><span class="line">    server 192.168.0.121;</span><br><span class="line">    server 192.168.0.122;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>ip_hash</code> 会基于客户端 IP 做哈希，让同一个用户尽量落到同一台后端。这个常用于保持 session 一致性</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">后端状态有：</span><br><span class="line">down    不参与处理请求</span><br><span class="line">backup  其他机器忙或不可用时才参与</span><br><span class="line">weight  权重</span><br></pre></td></tr></table></figure><h2 id="Keepalived：解决分发器单点故障"><a href="#Keepalived：解决分发器单点故障" class="headerlink" title="Keepalived：解决分发器单点故障"></a>Keepalived：解决分发器单点故障</h2><p>后端业务服务器挂了，Nginx upstream 可以临时绕开，但如果前端分发器本身挂了，就需要高可用</p><p>这时就用到 Keepalived ， Keepalived 基于 VRRP 协议，实现 VIP 漂移</p><p>比如一个 keepalived 结构：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Nginx-1  MASTER</span><br><span class="line">Nginx-2  BACKUP</span><br><span class="line"> VIP      192.168.31.33</span><br></pre></td></tr></table></figure><p>用户访问 VIP，正常情况下 VIP 在 MASTER 上，MASTER 挂了，BACKUP 接管 VIP</p><p>Keepalived 默认通过 VRRP 多播发送通告。MASTER 定时告诉 BACKUP：我还活着。如果 BACKUP 一段时间收不到，就会参与选举并接管 VIP</p><p>云主机环境通常不支持这种二层 VIP 漂移，云厂商一般提供 SLB&#x2F;负载均衡产品，所以云上直接用 SLB 更合适</p><p>MASTER 示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">global_defs &#123;</span><br><span class="line">    router_id wb01</span><br><span class="line">&#125;                      当前机器的唯一标识</span><br><span class="line"></span><br><span class="line">vrrp_script chk_nginx &#123;</span><br><span class="line">    script &quot;/etc/keepalived/nginx_check.sh&quot;</span><br><span class="line">    interval 1</span><br><span class="line">&#125;                       定义一个脚本，每秒执行一次</span><br><span class="line"></span><br><span class="line">vrrp_instance VI_1 &#123;</span><br><span class="line">    state MASTER                master</span><br><span class="line">    interface ens33             本机网卡</span><br><span class="line">    virtual_router_id 33        vip标识</span><br><span class="line">    priority 150                优先级，数值越大越高</span><br><span class="line">    advert_int 1           </span><br><span class="line"></span><br><span class="line">    authentication &#123;         </span><br><span class="line">        auth_type PASS</span><br><span class="line">        auth_pass 1111</span><br><span class="line">    &#125;                          验证方式</span><br><span class="line"></span><br><span class="line">    track_script &#123;</span><br><span class="line">        chk_nginx              使用脚本</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    virtual_ipaddress &#123;</span><br><span class="line">        192.168.31.33/24 dev ens33 label ens33:3         </span><br><span class="line">    &#125;                            vip在本机的配置</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>BACKUP 示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">global_defs &#123;</span><br><span class="line">    router_id wb02</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">vrrp_instance VI_1 &#123;</span><br><span class="line">    state BACKUP</span><br><span class="line">    interface ens33</span><br><span class="line">    virtual_router_id 33        与 master 一致</span><br><span class="line">    priority 100                 比 master 低</span><br><span class="line">    advert_int 1</span><br><span class="line"></span><br><span class="line">    authentication &#123;</span><br><span class="line">        auth_type PASS</span><br><span class="line">        auth_pass 1111</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    virtual_ipaddress &#123;</span><br><span class="line">        192.168.31.33/24 dev ens33 label ens33:3      虚拟ip配置</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以下四个配置必须一致</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">virtual_router_id</span><br><span class="line">authentication</span><br><span class="line">virtual_ipaddress</span><br><span class="line">advert_int</span><br></pre></td></tr></table></figure><p>而以下三个一般不同</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">router_id          唯一</span><br><span class="line">priority        MASTER 通常高于 BACKUP</span><br><span class="line">interface        真实网卡</span><br></pre></td></tr></table></figure><p>因为 Keepalived 只能保证主机存活而不能与 Nginx 产生联动，所以需要在 Master 上添加一个脚本来时刻检测 Nginx 的状态，当 Nginx 出现问题时就移交 Vip</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">#!/bin/bash</span><br><span class="line">pidof nginx &gt;/dev/null 2&gt;&amp;1</span><br><span class="line">if [ $? -ne 0 ]; then</span><br><span class="line">    sleep 1</span><br><span class="line">fi</span><br><span class="line">exit 0</span><br></pre></td></tr></table></figure><h3 id="裂脑问题"><a href="#裂脑问题" class="headerlink" title="裂脑问题"></a>裂脑问题</h3><p>裂脑就是两台机器都认为自己是 MASTER，于是同时持有同一个 VIP，结果就是同一个 IP 在网络里对应两个 MAC，访问混乱</p><p>常见原因：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">心跳链路问题</span><br><span class="line">网卡故障</span><br><span class="line">交换机故障</span><br><span class="line">VRRP 报文被拦截</span><br><span class="line">配置不一致</span><br></pre></td></tr></table></figure><p>缓解方式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">增加冗余心跳链路</span><br><span class="line">监控 VIP 是否同时存在</span><br><span class="line">监控 BACKUP 是否在 MASTER 正常时错误持有 VIP</span><br></pre></td></tr></table></figure><p>比如：在备节点上检查，如果能连通主节点 80 端口，并且备节点自己又持有 VIP，就报警，因为主节点 Web 服务正常时，备节点不应该接管 VIP。</p>]]>
    </content>
    <id>https://threcial.github.io/web%E6%9C%8D%E5%8A%A1/nginx-%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E9%85%8D%E7%BD%AE/</id>
    <link href="https://threcial.github.io/web%E6%9C%8D%E5%8A%A1/nginx-%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E9%85%8D%E7%BD%AE/"/>
    <published>2026-05-31T05:15:04.000Z</published>
    <summary>Nginx 默认参数偏保守，适合最小硬件环境。真实业务里通常会调整 高并发优化：worker 和连接数 worker_processes auto; events { worker_connectio</summary>
    <title>Nginx 性能优化配置</title>
    <updated>2026-05-31T05:15:04.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="web服务" scheme="https://threcial.github.io/categories/web%E6%9C%8D%E5%8A%A1/"/>
    <category term="nginx" scheme="https://threcial.github.io/tags/nginx/"/>
    <category term="配置" scheme="https://threcial.github.io/tags/%E9%85%8D%E7%BD%AE/"/>
    <content>
      <![CDATA[<h2 id="访问控制-allow-deny-和-auth-basic"><a href="#访问控制-allow-deny-和-auth-basic" class="headerlink" title="访问控制 allow &#x2F; deny 和 auth_basic"></a>访问控制 allow &#x2F; deny 和 auth_basic</h2><p>Nginx 可以按 IP 做访问控制：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">location /a/ &#123;</span><br><span class="line">    allow 127.0.0.1;</span><br><span class="line">    allow 117.51.154.71;</span><br><span class="line">    allow 192.168.31.0/24;</span><br><span class="line">    deny all;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>顺序是从上往下判断。上面这段意思是：允许本机、指定公网 IP、指定网段访问，其他全部拒绝。</p><p>如果不是按 IP，而是要求输入用户名密码，就用 Basic Auth：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">location /b/ &#123;</span><br><span class="line">    auth_basic &quot;test&quot;;    test只是一个名字，可以任意</span><br><span class="line">    auth_basic_user_file /opt/nginx/passwd/htpasswd;  密码文件路径</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>密码文件可以用 <code>htpasswd</code> 生成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">yum install httpd-tools -y                     下载 httpasswd</span><br><span class="line">mkdir -p /opt/nginx/passwd                     创建一个放密码文件的目录</span><br><span class="line">htpasswd -c /opt/nginx/passwd/htpasswd user1   -c 创建一个密码文件并添加user1用户</span><br><span class="line">htpasswd -m /opt/nginx/passwd/htpasswd user2   -m 添加用户，-c 会重新创建</span><br></pre></td></tr></table></figure><p><strong><code>-c</code> 是创建文件，已有文件后再加用户不要重复用 <code>-c</code>，否则会覆盖原文件</strong></p><h2 id="防盗链"><a href="#防盗链" class="headerlink" title="防盗链"></a>防盗链</h2><p>防盗链原理：根据 HTTP 请求头里的 <code>Referer</code> 判断这个资源是不是从允许的页面引用过来的</p><p>典型配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">location ~* \.(png)$ &#123;</span><br><span class="line">    valid_referers none blocked 192.168.31.20;</span><br><span class="line"></span><br><span class="line">    if ($invalid_referer) &#123;</span><br><span class="line">        return 403;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>valid_referers</code> 会根据 Referer 头设置 <code>$invalid_referer</code></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">none          允许没有 Referer</span><br><span class="line">blocked       允许 Referer 被代理或防火墙隐藏的情况</span><br><span class="line">server_names  允许 server_name 中的主机名</span><br></pre></td></tr></table></figure><p>如果 Referer 不符合规则，<code>$invalid_referer</code> 就是 1，于是返回 403</p><p>这个功能适合减少图片、音视频等资源被别人页面直接引用。它不是强安全手段，因为 Referer 可以伪造，但对普通盗链和爬虫有一定效果</p><h2 id="虚拟主机"><a href="#虚拟主机" class="headerlink" title="虚拟主机"></a>虚拟主机</h2><p>虚拟主机的本质是：一台真实服务器上跑多个逻辑网站</p><p>Nginx 的匹配逻辑可以简化为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">先看连接进入哪个 listen IP:port</span><br><span class="line">再看 Host 匹配哪个 server_name</span><br><span class="line">匹配不到就走 default_server 或当前 listen 组第一个 server</span><br></pre></td></tr></table></figure><p>所以可以基于三种方式区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">IP</span><br><span class="line">端口</span><br><span class="line">域名</span><br></pre></td></tr></table></figure><h3 id="基于-IP"><a href="#基于-IP" class="headerlink" title="基于 IP"></a>基于 IP</h3><p>需要本机有多个 IP</p><p>配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    listen 192.168.0.129:80;</span><br><span class="line">    location / &#123;</span><br><span class="line">       root /opt/nginx/html/web01;</span><br><span class="line">       index index.html;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">server &#123;</span><br><span class="line">    listen 192.168.0.130:80;</span><br><span class="line">    location / &#123;</span><br><span class="line">       root /opt/nginx/html/web02;</span><br><span class="line">       index index.html;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>访问不同 IP，进入不同目录，这种方式成本高，因为真实公网环境需要多个 IP，所以企业里不常作为首选</p><h3 id="基于端口"><a href="#基于端口" class="headerlink" title="基于端口"></a>基于端口</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    listen 80;</span><br><span class="line">    location / &#123;</span><br><span class="line">        root /opt/nginx/html/web01;</span><br><span class="line">        index index.html;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">server &#123;</span><br><span class="line">    listen 8080;</span><br><span class="line">    location / &#123;</span><br><span class="line">        root /opt/nginx/html/web02;</span><br><span class="line">       index index.html;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>访问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">http://192.168.0.129/</span><br><span class="line">http://192.168.0.129:8080/</span><br></pre></td></tr></table></figure><p>端口型虚拟主机更适合内部服务，公网用户一般不喜欢记端口</p><h3 id="基于域名"><a href="#基于域名" class="headerlink" title="基于域名"></a>基于域名</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    listen 80;</span><br><span class="line">    server_name www.abc.com;</span><br><span class="line"></span><br><span class="line">    location /web1 &#123;</span><br><span class="line">       root html;</span><br><span class="line">        index index.html;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">server &#123;</span><br><span class="line">    listen 80;</span><br><span class="line">    server_name www.aaa.com;</span><br><span class="line"></span><br><span class="line">    location /web2 &#123;</span><br><span class="line">        root html;</span><br><span class="line">        index index.html;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>域名型最符合真实业务使用方式，测试时要配 hosts 或 DNS，让域名解析到 Nginx 服务器</p><h2 id="反向代理"><a href="#反向代理" class="headerlink" title="反向代理"></a>反向代理</h2><p>正向代理代理的是客户端</p><p>客户端必须知道代理服务器地址，并主动配置。VPN、公司出口代理、某些软件代理都属于这个方向，VPN 还通常带有加密隧道，不只是简单 HTTP 代理</p><p>反向代理代理的是服务端</p><p>用户不知道后端是谁，只知道访问 Nginx ，Nginx 再把请求转发到内部服务器</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    location / &#123;</span><br><span class="line">        proxy_pass http://192.168.31.23:80;</span><br><span class="line">        proxy_set_header X-Real-IP $remote_addr;</span><br><span class="line">        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果后端要记录真实客户端 IP，需要日志格式配合：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">log_format main &#x27;$http_x_real_ip|$http_x_forwarded_for - $remote_user [$time_local] &quot;$request&quot; &#x27;</span><br><span class="line">                &#x27;$status $body_bytes_sent &quot;$http_referer&quot; &#x27;</span><br><span class="line">                &#x27;&quot;$http_user_agent&quot; &quot;$http_x_forwarded_for&quot;&#x27;;</span><br></pre></td></tr></table></figure><p>否则后端看到的来源只是前端 Nginx</p><h2 id="限速"><a href="#限速" class="headerlink" title="限速"></a>限速</h2><p>Nginx 限速涉及三个概念：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">limit_req   限制请求频率</span><br><span class="line">limit_conn  限制并发连接</span><br><span class="line">limit_rate  限制响应传输速度</span><br></pre></td></tr></table></figure><p>使用 <code>limit_req</code> 和 <code>limit_conn</code> 需要提前创建区域，而 <code>limit_rate</code> 不需要</p><p><code>limit_req</code> 基于漏桶算法。请求像水一样进入桶，Nginx 按固定速度处理，超过容量就丢弃</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">limit_req_zone $binary_remote_addr zone=myzone:10m rate=1r/s;</span><br><span class="line">                 </span><br><span class="line">location / &#123;</span><br><span class="line">    limit_req zone=myzone burst=5 nodelay;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">创建区域，根据二进制ip分类，区域名 myzone 10m空间 每秒一次请求</span><br><span class="line"></span><br><span class="line">使用 myzone 区域</span><br><span class="line">允许突发 5 个</span><br><span class="line">nodelay 表示超过突发能力后直接返回 503</span><br></pre></td></tr></table></figure><p>下载限制通常用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">limit_conn_zone $binary_remote_addr zone=down:10m;</span><br><span class="line"></span><br><span class="line">location /download/ &#123;</span><br><span class="line">   limit_conn down 1;</span><br><span class="line">   limit_rate 10k;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">limit_conn down 1 表示同一个 IP 同时只能有 1 个连接进入这个限制区域</span><br><span class="line">limit_rate 10k 是每连接限速，不是每用户总限速。如果一个用户开多个连接，总速度会叠加，所以下载限速通常要和 limit_conn 一起配</span><br></pre></td></tr></table></figure><p>如果把下载限制写在 <code>location /</code>，下载和网页都会抢同一个连接额度，所以下载资源要放到单独目录进行限速</p><h2 id="URL-重写-与-rewrite-模块"><a href="#URL-重写-与-rewrite-模块" class="headerlink" title="URL 重写 与 rewrite 模块"></a>URL 重写 与 rewrite 模块</h2><p>Nginx 的 rewrite 基于 PCRE 正则，用于 URL 重写、跳转和内部 URI 改写</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">语法：</span><br><span class="line">rewrite 正则 替换内容 flag;</span><br></pre></td></tr></table></figure><p>常见 flag：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">permanent  301 永久重定向，浏览器地址栏变化</span><br><span class="line">redirect   302 临时重定向，浏览器地址栏变化</span><br><span class="line">last       停止当前 rewrite 指令集，用新 URI 重新匹配 location</span><br><span class="line">break      停止当前 rewrite 指令集，不重新匹配 location</span><br></pre></td></tr></table></figure><p>如果替换内容以 <code>http://</code>、<code>https://</code> 或 <code>$scheme</code> 开头，也会变成外部跳转，默认临时重定向</p><p>如果不写 flag，容易按默认行为继续处理，实际效果接近继续走 rewrite 流程</p><p><strong>目录一定要小心结尾 &#x2F;</strong></p><p>如果 rewrite 到的是目录，最好写成如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rewrite passwd$ /dir/ last;</span><br></pre></td></tr></table></figure><p>而不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rewrite passwd$ /dir last;</span><br></pre></td></tr></table></figure><p>因为 <code>/dir</code> 如果对应文件系统里的目录，Nginx 可能自动返回 301，把地址补成 <code>/dir/</code>。这时浏览器地址栏会变化，而 <code>break</code> 或 <code>last</code> 跳转的跳转并不会改变浏览器地址栏，同时因为是目录规范化触发了 301，后续 flag 并不会生效</p><h3 id="rewrite-里的变量、if、return、break"><a href="#rewrite-里的变量、if、return、break" class="headerlink" title="rewrite 里的变量、if、return、break"></a><strong>rewrite 里的变量、if、return、break</strong></h3><p>Nginx 变量都是字符串。可以用 <code>set</code> 定义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">set $test 1111;</span><br><span class="line">rewrite ^(.*)$ http://192.168.111.12/$test/ permanent;</span><br></pre></td></tr></table></figure><p>变量作用范围和定义位置有关：在 <code>server</code> 里定义，该 server 内的 location 可以用；在 <code>location</code> 里定义，主要就在当前 location 中使用</p><p><code>if</code> 可以根据变量判断：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">if ($http_user_agent ~* &#x27;elinks&#x27;) &#123;</span><br><span class="line">    return 404;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这表示 User-Agent 包含 elinks 就返回 404</p><p><code>return</code> 也是 rewrite 模块的指令，它会直接结束请求</p><p><code>break</code> 则不结束请求，只是停止后续 rewrite 模块指令：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">if ($http_user_agent ~* &#x27;elinks&#x27;) &#123;</span><br><span class="line">    break;</span><br><span class="line">    return 404;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段里 <code>break</code> 执行后，后面的 <code>return 404</code> 不会执行。请求并没有被拒绝，而是继续走当前 location 的普通内容处理，所以最后可能仍然返回正常页面</p><p><strong>break 只是不再执行 rewrite 指令，如 return set if 等，其余不影响</strong></p><p><strong>不要把 Nginx 配置理解成 Shell 脚本</strong></p>]]>
    </content>
    <id>https://threcial.github.io/web%E6%9C%8D%E5%8A%A1/nginx-%E5%8A%9F%E8%83%BD%E9%85%8D%E7%BD%AE/</id>
    <link href="https://threcial.github.io/web%E6%9C%8D%E5%8A%A1/nginx-%E5%8A%9F%E8%83%BD%E9%85%8D%E7%BD%AE/"/>
    <published>2026-05-30T12:34:05.000Z</published>
    <summary>访问控制 allow / deny 和 auth_basic Nginx 可以按 IP 做访问控制： location /a/ { allow 127.0.0.1; allow 117.51.154.</summary>
    <title>Nginx 功能配置</title>
    <updated>2026-05-30T12:34:05.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="web服务" scheme="https://threcial.github.io/categories/web%E6%9C%8D%E5%8A%A1/"/>
    <category term="nginx" scheme="https://threcial.github.io/tags/nginx/"/>
    <category term="配置" scheme="https://threcial.github.io/tags/%E9%85%8D%E7%BD%AE/"/>
    <content>
      <![CDATA[<p>Nginx 可以做 Web 服务器、反向代理服务器，也能承担邮件代理相关功能。它比较典型的特点是轻量、资源消耗低、并发能力强。</p><p>和 Apache 相比，Nginx 的优势主要来自事件驱动和异步非阻塞模型。Apache 更偏阻塞式处理，请求多了以后进程&#x2F;线程资源压力会变大；Nginx 借助 epoll 模型，用较少的 worker 来处理大量连接。</p><p>Nginx 是典型的 master + worker 模型。master 负责读取配置、管理 worker ，worker 负责处理请求。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">通过yum安装的nginx有以下四个重要目录</span><br><span class="line">/etc/nginx nginx所有的文件和配置都在这个目录</span><br><span class="line">/usr/share/nginx/html网站根目录</span><br><span class="line">/var/log/nginxnginx日志目录</span><br><span class="line">/var/cache/nginx                                缓存目录</span><br></pre></td></tr></table></figure><p>本文重点就在于 nginx 配置文件写法</p><h2 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h2><p>Nginx 配置不是平铺的，它按上下文划分，一个常见的配置文件 nginx.conf 如</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">user  nginx;                            指定启动Nginx工作进程的用户</span><br><span class="line">worker_processes  auto;    工作进程的数量，进程数，建议设置为auto，由nginx自己配置</span><br><span class="line">           </span><br><span class="line">error_log  logs/error.log  error;      错误日志及存放目录、日志级别</span><br><span class="line">#pid        logs/nginx.pid;               nginx启动之后pid存放位置</span><br><span class="line"></span><br><span class="line">events &#123;</span><br><span class="line">    worker_connections  1024;            每个进程的连接数</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">http &#123;                                   http模块设置</span><br><span class="line">   nginx支持类型</span><br><span class="line">    include       mime.types;</span><br><span class="line">    default_type  application/octet-stream;</span><br><span class="line"></span><br><span class="line">    #log_format  main  &#x27;$remote_addr - $remote_user [$time_local] &quot;$request&quot; &#x27;</span><br><span class="line">    #                  &#x27;$status $body_bytes_sent &quot;$http_referer&quot; &#x27;</span><br><span class="line">    #                  &#x27;&quot;$http_user_agent&quot; &quot;$http_x_forwarded_for&quot;&#x27;;</span><br><span class="line"></span><br><span class="line">    #access_log  logs/access.log  main;      访问日志 目录与格式</span><br><span class="line"></span><br><span class="line">    sendfile        on;               零拷贝</span><br><span class="line">    #tcp_nopush     on;               连接超时，时间 单位s</span><br><span class="line">    #keepalive_timeout  0;</span><br><span class="line">    keepalive_timeout  65;</span><br><span class="line">    #gzip  on;                      开启gzip压缩</span><br><span class="line">    server &#123;                             一个虚拟主机</span><br><span class="line">        listen       80;                监听端口</span><br><span class="line">        server_name  localhost;                    域名</span><br><span class="line">#charset koi8-r;                            字符集</span><br><span class="line">        #access_log  logs/host.access.log  main;    定义访问日志</span><br><span class="line">        location / &#123;                              默认请求</span><br><span class="line">            root   html;                     定义虚拟主机默认网站的目录</span><br><span class="line">            index  index.html index.htm;     定义首页索引文件的名称</span><br><span class="line">            &#125;</span><br><span class="line">        #error_page  404              /404.html;    </span><br><span class="line">        # redirect server error pages to the static page /50x.html</span><br><span class="line">        error_page   500 502 503 504  /50x.html;</span><br><span class="line">        location = /50x.html &#123;</span><br><span class="line">            root   html;</span><br><span class="line">        &#125;  </span><br></pre></td></tr></table></figure><p><code>events</code> 里配置连接能力</p><p><code>http</code> 里是 HTTP 层配置，比如日志格式、gzip、sendfile、server、include 等</p><p><code>server</code> 是虚拟主机</p><p><code>location</code> 是 URI 匹配规则</p><h2 id="日志"><a href="#日志" class="headerlink" title="日志"></a>日志</h2><p>Nginx 有访问日志和错误日志</p><p>错误日志级别有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">debug</span><br><span class="line">info</span><br><span class="line">notice</span><br><span class="line">warn</span><br><span class="line">error</span><br><span class="line">crit</span><br><span class="line">alert</span><br><span class="line">emerg</span><br></pre></td></tr></table></figure><p>级别越高，记录越少。生产里常用 <code>warn</code>、<code>error</code>、<code>crit</code>，不要轻易开 <code>info</code> 或 <code>debug</code>，否则磁盘 IO 很容易被日志拖住</p><p>访问日志通常通过 <code>log_format</code> 定义格式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">log_format main &#x27;$remote_addr - $remote_user [$time_local] &quot;$request&quot; &#x27;</span><br><span class="line">               &#x27;$status $body_bytes_sent &quot;$http_referer&quot; &#x27;</span><br><span class="line">               &#x27;&quot;$http_user_agent&quot; &quot;$http_x_forwarded_for&quot;&#x27;;</span><br><span class="line"></span><br><span class="line">$remote_addr              客户端 IP</span><br><span class="line">$http_x_forwarded_for     代理链路里的真实客户端信息</span><br><span class="line">$remote_user              认证用户</span><br><span class="line">$time_local               访问时间</span><br><span class="line">$request                  请求行</span><br><span class="line">$status                   状态码</span><br><span class="line">$body_bytes_sent          响应体大小</span><br><span class="line">$http_referer             来源页面</span><br><span class="line">$http_user_agent          浏览器信息</span><br></pre></td></tr></table></figure><h2 id="include"><a href="#include" class="headerlink" title="include"></a>include</h2><p>Nginx 主配置文件不应该无限膨胀，常见写法是在 <code>http</code> 块里加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">include /etc/nginx/conf.d/*.conf;</span><br></pre></td></tr></table></figure><p>这样 <code>/etc/nginx/conf.d/</code> 下所有 <code>.conf</code> 文件都会被包含进来</p><h2 id="location-块"><a href="#location-块" class="headerlink" title="location 块"></a>location 块</h2><p>nginx有两层指令来匹配请求 URI 。第一个层次是 server 指令，它通过域名、ip 和端口来做第一层级匹配，当找到匹配的 server 后就进入此 server 的 location 匹配。</p><p>location 的匹配并不完全按照其在配置文件中出现的顺序来匹配，请求URI 会按如下规则进行匹配：</p><ol><li>先精准匹配 <strong><code>=</code></strong> ，精准匹配成功则会立即停止其他类型匹配</li><li>没有精准匹配成功时，进行带有 <code>^~</code> 的前缀匹配</li><li><strong><code>=</code></strong> 和 <strong><code>^~</code></strong> 均未匹配成功前提下，查找正则匹配 <strong><code>~</code></strong> 和 <strong><code>~*</code></strong> 。当同时有多个正则匹配时，按其在配置文件中出现的先后顺序优先匹配，命中则立即停止其他类型匹配</li><li>以上都未成功时进行普通前缀匹配，多个匹配成功时以最长前缀为准</li><li>默认匹配规则进行兜底，即使配置文件里未有默认匹配 location 块，nginx 依旧会用默认规则处理</li></ol><p>以上规则简单总结就是优先级从高到低依次为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1. location =    精准匹配</span><br><span class="line">2. location ^~   带参前缀匹配</span><br><span class="line">3. location ~  或 ~*  正则匹配</span><br><span class="line">4. location /a   普通前缀匹配，优先级低于带参数前缀匹配</span><br><span class="line">5. location /    默认规则，任何没有匹配成功的，都会匹配这里处理</span><br></pre></td></tr></table></figure><p><strong><code>root</code> 和 <code>alias</code> 的区别</strong></p><p><code>root</code> 是把 URI 拼到 root 后面：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">location /img/ &#123;</span><br><span class="line">    root /data/www;</span><br><span class="line">&#125;</span><br><span class="line">访问      /img/a.png</span><br><span class="line">实际路径  /data/www/img/a.png</span><br></pre></td></tr></table></figure><p><code>alias</code> 是用指定路径替换 location 前缀：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">location /img/ &#123;</span><br><span class="line">    alias /data/images/;</span><br><span class="line">&#125;</span><br><span class="line">访问      /img/a.png</span><br><span class="line">实际路径  /data/images/a.png</span><br></pre></td></tr></table></figure><p>所以 <code>alias</code> 更像“映射到另一个目录”，<code>root</code> 更像“以这个目录为根再拼 URI”</p>]]>
    </content>
    <id>https://threcial.github.io/web%E6%9C%8D%E5%8A%A1/nginx-%E5%9F%BA%E6%9C%AC%E9%85%8D%E7%BD%AE/</id>
    <link href="https://threcial.github.io/web%E6%9C%8D%E5%8A%A1/nginx-%E5%9F%BA%E6%9C%AC%E9%85%8D%E7%BD%AE/"/>
    <published>2026-05-30T09:11:24.000Z</published>
    <summary>Nginx 可以做 Web 服务器、反向代理服务器，也能承担邮件代理相关功能。它比较典型的特点是轻量、资源消耗低、并发能力强。 和 Apache 相比，Nginx 的优势主要来自事件驱动和异步非阻塞模</summary>
    <title>nginx 基本配置</title>
    <updated>2026-05-30T09:11:24.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="centos7" scheme="https://threcial.github.io/tags/centos7/"/>
    <category term="安装" scheme="https://threcial.github.io/tags/%E5%AE%89%E8%A3%85/"/>
    <content>
      <![CDATA[<p>CentOS Linux 7 已经在 2024 年 6 月 30 日结束生命周期，不再获得官方更新和安全补丁，实际中应使用其他版本，但安装思路是相近的</p><h2 id="u盘启动"><a href="#u盘启动" class="headerlink" title="u盘启动"></a><strong>u盘启动</strong></h2><p>进入系统 bios 界面</p><p>启动参数里写的 ISO 卷标，和实际 U 盘的卷标可能不一致，而 CentOS 安装程序会根据 <code>LABEL=</code> 去找安装源，如果 U 盘卷标对不上，就会找不到</p><p>常见启动参数类似 <code>inst.stage2=hd:LABEL=CentOS\x207\x20x86_64 quiet</code>，需要修改成u盘卷标</p><h2 id="分区"><a href="#分区" class="headerlink" title="分区"></a>分区</h2><p>大于 2T 的硬盘要注意 MBR 和 GPT。MBR 最大只能识别到 2T 左右，大容量硬盘应该使用 GPT 分区</p><p>如果是服务器，UEFI 启动，需要单独创建 &#x2F;boot&#x2F;efi 500M</p><p>如果是 Legacy BIOS + GPT，需要 BIOS Boot 分区，也就是 &#x2F;biosboot</p><p>一般 &#x2F;boot 分区即可</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">/boot 1G</span><br><span class="line">/boot/efi 500M</span><br><span class="line">swap 实际内存的两倍，不超过16G</span><br><span class="line">/ 200G</span><br><span class="line">/data 剩余所有空间</span><br></pre></td></tr></table></figure><h2 id="系统优化"><a href="#系统优化" class="headerlink" title="系统优化"></a>系统优化</h2><ol><li><p><strong>安装好后首先确保网络正常</strong></p></li><li><p><strong>关闭 SELinux</strong>  </p><p>编辑配置文件：<code>vi /etc/selinux/config</code><br>修改 <code>SELINUX=disable</code></p></li><li><p><strong>根据实际情况开关防火墙</strong></p></li><li><p><strong>修改 yum 源并安装扩展源</strong>  </p><p><code>curl -s -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-7.repo   yum install epel-release -y</code></p></li><li><p><strong>安装常用软件、更新系统</strong>  </p><p><code>yum groupinstall &quot;Compatibility libraries&quot; &quot;Base&quot; &quot;Development tools&quot; -y   yum groupinstall &quot;debugging Tools&quot; &quot;Dial-up Networking Support&quot; -y   yum install tree nmap dos2unix lrzsz nc lsof wget tcpdump htop iftop iotop sysstat nethogs -y   yum install psmisc net-tools bash-completion vim-enhanced -y   yum update -y</code></p></li><li><p><strong>精简开机</strong>  </p><p><code>systemctl list-unit-files |grep enable|egrep -v &quot;sshd.service|crond.service|sysstat|rsyslog|^NetworkManager.service|irqbalance.service&quot;|awk &#39;{print &quot;systemctl disable&quot;,$1}&#39;|bash</code></p></li><li><p><strong>设置时间同步自动任务</strong>  </p><p>在计划任务里面添加 <code>0 */12 * * * /usr/sbin/ntpdate ntp3.aliyun.com &gt;/dev/null 2&gt;&amp;1</code></p></li><li><p><strong>修改文件描述符限制</strong>  </p><p>查看当前限制：<code>ulimit -n</code><br>如果小于 65535，修改：<code>vi /etc/security/limits.conf</code><br>添加：<code>* - nofile 65535</code></p></li><li><p><strong>优化系统内核</strong>  </p><p>编辑：<code>vi /etc/sysctl.conf</code><br>添加：<br><code>net.ipv4.tcp_keepalive_time = 1800   net.ipv4.ip_local_port_range = 4000 65000   net.ipv4.tcp_max_syn_backlog = 8192   net.core.somaxconn = 16384   net.core.netdev_max_backlog = 16384</code></p></li><li><p><strong>清除系统版本信息</strong>  </p><p><code>&gt;/etc/issue</code><br><code>&gt;/etc/issue.net</code></p></li><li><p><strong>配置好 ssh 后修改 ssh 配置文件</strong></p></li><li><p><strong>锁定关键文件</strong>  </p><p>chattr +i &#x2F;etc&#x2F;passwd<br>chattr +i &#x2F;etc&#x2F;shadow</p></li></ol><h2 id="vmware-上克隆模板机"><a href="#vmware-上克隆模板机" class="headerlink" title="vmware 上克隆模板机"></a>vmware 上克隆模板机</h2><p>在启动虚拟机之前修改 MAC 地址</p><p>修改主机名 vim &#x2F;etc&#x2F;hostname</p><p>修改网卡的 uuid 和 ip 地址 vim &#x2F;etc&#x2F;sysconfig&#x2F;network-scripts&#x2F;ifcfg-ens33 ，uuid 通过命令 uuidgen 生成</p>]]>
    </content>
    <id>https://threcial.github.io/linux/%E5%9C%A8%E6%9C%8D%E5%8A%A1%E5%99%A8%E4%B8%8A%E5%AE%89%E8%A3%85-centos-7/</id>
    <link href="https://threcial.github.io/linux/%E5%9C%A8%E6%9C%8D%E5%8A%A1%E5%99%A8%E4%B8%8A%E5%AE%89%E8%A3%85-centos-7/"/>
    <published>2026-05-24T12:08:10.000Z</published>
    <summary>CentOS Linux 7 已经在 2024 年 6 月 30 日结束生命周期，不再获得官方更新和安全补丁，实际中应使用其他版本，但安装思路是相近的 u盘启动 进入系统 bios 界面 启动参数里写</summary>
    <title>在服务器上安装 centos-7</title>
    <updated>2026-05-24T12:08:10.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="文本处理" scheme="https://threcial.github.io/tags/%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86/"/>
    <content>
      <![CDATA[<p><strong>grep：找出符合条件的行</strong>，核心思路是：哪些行符合条件</p><p><strong>sed：对行内容进行增删改查</strong>，核心思路是：按规则修改某些行</p><p><strong>awk：按列处理文本</strong>，核心思路是：按字段、按列提取或计算数据，适合处理有规律的字段</p><h2 id="grep"><a href="#grep" class="headerlink" title="grep"></a>grep</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">grep [选项] 文件</span><br><span class="line">    -v 取反</span><br><span class="line">    -i 不区分大小写</span><br><span class="line">    -n 显示行号</span><br><span class="line">    -w 只匹配单词，单词之间可以是任意分割符</span><br><span class="line">    -o 只显示查找内容，与正则一起使用</span><br><span class="line">    -E 使用扩展正则表达式</span><br></pre></td></tr></table></figure><h2 id="sed"><a href="#sed" class="headerlink" title="sed"></a>sed</h2><p>sed 的全名为 stream editor，流编辑器，适合编辑文件。sed 默认不会修改原文件，只会把处理结果输出到屏幕</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">sed [选项] [sed内置命令字符] [文件]</span><br><span class="line">  选项</span><br><span class="line">    -n 取消命令默认输出，默认输出是对每一行处理后都输出</span><br><span class="line">    -i 修改文件，建议使用 -i.bak 修改时能够备份    </span><br><span class="line">    -e 多次编辑，或者使用 ; 来执行多个命令</span><br><span class="line">    -E 使用扩展正则表达式</span><br><span class="line">  内置命令字符         </span><br><span class="line">    s 替换         </span><br><span class="line">    g 全局         </span><br><span class="line">    p 打印</span><br><span class="line">    d 删除</span><br><span class="line">    a 增加,在指定行后面添加一行或者多行文本</span><br><span class="line">    i 插入，在指定行前面添加一行或者多行文本</span><br></pre></td></tr></table></figure><h2 id="awk"><a href="#awk" class="headerlink" title="awk"></a>awk</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">awk &#x27;条件 &#123;动作&#125;&#x27; 文件     注意：由于动作中有$符号，需要用单引号，否则会被shell解析</span><br><span class="line">  -F 指定分隔符，默认空格</span><br><span class="line">  动作</span><br><span class="line">    $0 表示整行</span><br><span class="line">    $1 表示第一列</span><br><span class="line">    $2 表示第二列</span><br><span class="line">    $NF 表示最后一列</span><br><span class="line">    NR 表示当前行号</span><br><span class="line"></span><br><span class="line">awk &#x27;&#123;print $1&#125;&#x27; linux.txt     打印第一列</span><br><span class="line">awk &#x27;&#123;print $NF&#125;&#x27; linux.txt    打印最后一列</span><br><span class="line"></span><br><span class="line">awk &quot;NR==3&quot; file                打印第三行</span><br><span class="line">awk -F &quot;:&quot; &#x27;NR==2&#123;print $3&#125;&#x27; /etc/passwd      打印第二行第3列</span><br><span class="line"></span><br><span class="line">awk -F &quot;:&quot; &#x27;/shutdown/&#123;print $NF&#125;&#x27; /etc/passwd   包含shutdown的行的最后一列</span><br><span class="line">awk -F &quot;:&quot; &#x27;$1~/root/&#123;print $NF&#125;&#x27; /etc/passwd    第一列有root的行的最后一列</span><br><span class="line">awk -F &quot;:&quot; &#x27;$1!~/root/&#123;print $NF&#125;&#x27; /etc/passwd   !取反 ~表示正则</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/linux/%E5%A4%8D%E6%9D%82%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86-grep-sed-awk/</id>
    <link href="https://threcial.github.io/linux/%E5%A4%8D%E6%9D%82%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86-grep-sed-awk/"/>
    <published>2026-05-24T10:58:32.000Z</published>
    <summary>grep：找出符合条件的行，核心思路是：哪些行符合条件 sed：对行内容进行增删改查，核心思路是：按规则修改某些行 awk：按列处理文本，核心思路是：按字段、按列提取或计算数据，适合处理有规律的字段</summary>
    <title>复杂文本处理-grep sed awk</title>
    <updated>2026-05-24T10:58:32.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="正则表达式" scheme="https://threcial.github.io/tags/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/"/>
    <content>
      <![CDATA[<p>Linux 中最常见的文本处理工具是：grep、sed、awk，也就是常说的“文本三剑客”</p><ul><li><strong>grep</strong>：查找、过滤符合条件的行</li><li><strong>sed</strong>：按行编辑、替换、删除、插入文本</li><li><strong>awk</strong>：按列处理、条件判断、统计计算</li></ul><p>正则表达式就是给这些工具提供匹配规则的语言，它不是普通命令本身，而是一套用于描述文本模式的规则</p><p><strong>正则表达式和 Shell 通配符</strong>：两者会使用相同的符号，这些符号也有相近的意思，但二者完全不相同，需要注意区分</p><p>Linux 中常见的正则表达式主要分为两类：</p><ul><li><strong>BRE：Basic Regular Expression</strong>，基础正则表达式</li><li><strong>ERE：Extended Regular Expression</strong>，扩展正则表达式</li></ul><p>grep sed awk 默认都支持基础正则表达式，但是使用扩展正则表达式就需要使用参数</p><h2 id="基础正则常见符号"><a href="#基础正则常见符号" class="headerlink" title="基础正则常见符号"></a>基础正则常见符号</h2><h3 id="1-：匹配行首"><a href="#1-：匹配行首" class="headerlink" title="1. ^：匹配行首"></a>1. <code>^</code>：匹配行首</h3><p><code>^</code> 表示一行的开头</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep &#x27;^root&#x27; /etc/passwd      查找以 root 开头的行</span><br></pre></td></tr></table></figure><h3 id="2-：匹配行尾"><a href="#2-：匹配行尾" class="headerlink" title="2. $：匹配行尾"></a>2. <code>$</code>：匹配行尾</h3><p><code>$</code> 表示一行的结尾</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep &#x27;bash$&#x27; /etc/passwd    查找以 bash 结尾的行</span><br></pre></td></tr></table></figure><h3 id="3-：匹配空行"><a href="#3-：匹配空行" class="headerlink" title="3. ^$：匹配空行"></a>3. <code>^$</code>：匹配空行</h3><p><code>^</code> 是行首，<code>$</code> 是行尾，如果中间什么都没有就表示空行</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">^$</span><br></pre></td></tr></table></figure><h3 id="4-：匹配任意单个字符"><a href="#4-：匹配任意单个字符" class="headerlink" title="4. .：匹配任意单个字符"></a>4. <code>.</code>：匹配任意单个字符</h3><p><code>.</code> 表示任意一个字符，但必须有一个字符</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">grep &#x27;r..t&#x27; file.txt      查找 r t 之间存在两个字符的行</span><br><span class="line">grep &#x27;.&#x27; file.txt         查找任意非空行</span><br></pre></td></tr></table></figure><h3 id="5-：转义字符"><a href="#5-：转义字符" class="headerlink" title="5. \：转义字符"></a>5. <code>\</code>：转义字符</h3><p>有些字符在正则里有特殊含义，比如上述4个字符，如果想匹配它们本身，就需要使用反斜杠转义</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep &#x27;\.$&#x27; file.txt     匹配以点号结尾的行</span><br></pre></td></tr></table></figure><h3 id="6-：前一个字符重复-0-次或多次"><a href="#6-：前一个字符重复-0-次或多次" class="headerlink" title="6. *：前一个字符重复 0 次或多次"></a>6. <code>*</code>：前一个字符重复 0 次或多次</h3><p><code>*</code> 在通配符中表示任意内容，而在正则表达式中表示让前一个字符重复 0 次或多次</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">grep &#x27;go*d&#x27; file.txt</span><br><span class="line"></span><br><span class="line">可以匹配 gd god good 等</span><br></pre></td></tr></table></figure><h3 id="7-：任意长度内容"><a href="#7-：任意长度内容" class="headerlink" title="7. .*：任意长度内容"></a>7. <code>.*</code>：任意长度内容</h3><p>这是最常用的组合之一</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep &#x27;^.*n&#x27; linux.txt    表示从行首开始，匹配到最后一个 n 为止</span><br></pre></td></tr></table></figure><p><strong>注意：默认情况下，正则里的 <code>*</code> 通常是贪婪的，会尽可能多地匹配</strong></p><h3 id="8-：字符集合"><a href="#8-：字符集合" class="headerlink" title="8. []：字符集合"></a>8. <code>[]</code>：字符集合</h3><p><code>[]</code> 表示匹配集合中的任意一个字符</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">grep &#x27;[abc]&#x27; file.txt          匹配 a、b、c 中任意一个</span><br><span class="line">grep &#x27;[135acbyp]&#x27; file.txt     字符数字混合</span><br><span class="line">grep &#x27;[a-c]&#x27; file.txt          范围写法</span><br><span class="line">grep &#x27;[0-9]&#x27; file.txt          数字范围</span><br><span class="line">grep &#x27;[0-9a-zA-Z]&#x27; file.txt    多个范围</span><br></pre></td></tr></table></figure><h3 id="9-：字符集合取反"><a href="#9-：字符集合取反" class="headerlink" title="9. [^]：字符集合取反"></a>9. <code>[^]</code>：字符集合取反</h3><p><code>[^]</code> 表示匹配不在集合中的任意一个字符</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep &#x27;[^0-9]&#x27; file.txt         匹配包含有非数字的行</span><br></pre></td></tr></table></figure><h2 id="扩展正则常见符号"><a href="#扩展正则常见符号" class="headerlink" title="扩展正则常见符号"></a>扩展正则常见符号</h2><p>要使用扩展正则， grep 和 sed 需要使用 -E 参数，而 awk 可以直接使用</p><h3 id="1-：前一个字符出现-1-次或多次"><a href="#1-：前一个字符出现-1-次或多次" class="headerlink" title="1. +：前一个字符出现 1 次或多次"></a>1. <code>+</code>：前一个字符出现 1 次或多次</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">grep -E &#x27;go+d&#x27; file.txt</span><br><span class="line"></span><br><span class="line">可以匹配 god good goood 等</span><br></pre></td></tr></table></figure><h3 id="2-：前一个字符出现-0-次或-1-次"><a href="#2-：前一个字符出现-0-次或-1-次" class="headerlink" title="2. ?：前一个字符出现 0 次或 1 次"></a>2. <code>?</code>：前一个字符出现 0 次或 1 次</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">grep -E &#x27;go?d&#x27; file.txt</span><br><span class="line"></span><br><span class="line">可以匹配 gd god 仅此两个</span><br></pre></td></tr></table></figure><h3 id="3-：或者"><a href="#3-：或者" class="headerlink" title="3. |：或者"></a>3. <code>|</code>：或者</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">grep -E &#x27;linux|weibo&#x27; passwd    匹配包含 linux 或 weibo 的行</span><br><span class="line">grep &#x27;linux|weibo&#x27; passwd      如果不加 -E 就会查找  linux|weibo 而没有或者的含义</span><br></pre></td></tr></table></figure><h3 id="4-n-m-：控制重复次数"><a href="#4-n-m-：控制重复次数" class="headerlink" title="4. {n,m}：控制重复次数"></a>4. <code>{n,m}</code>：控制重复次数</h3><p><code>{}</code> 用于指定前一个字符或分组重复的次数</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">grep -E &#x27;[0-9]&#123;3&#125;&#x27; file.txt      匹配连续 3 个数字</span><br><span class="line">grep -E &#x27;[0-9]&#123;2,4&#125;&#x27; file.txt    匹配 2 到 4 个数字</span><br><span class="line">grep -E &#x27;[0-9]&#123;2,&#125;&#x27; file.txt     匹配至少 2 个数字</span><br><span class="line">grep -E &#x27;[0-9]&#123;,4&#125;&#x27; file.txt     匹配至多 4 个数字</span><br></pre></td></tr></table></figure><h3 id="5-：分组"><a href="#5-：分组" class="headerlink" title="5. ()：分组"></a>5. <code>()</code>：分组</h3><p><code>()</code> 可以把多个字符或表达式看成一个整体</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">grep -E &#x27;g(oo|ww)d&#x27; file.txt</span><br><span class="line"></span><br><span class="line">可以匹配 good 和 gwwd</span><br></pre></td></tr></table></figure><h3 id="6-1、-2：反向引用"><a href="#6-1、-2：反向引用" class="headerlink" title="6. \1、\2：反向引用"></a>6. <code>\1</code>、<code>\2</code>：反向引用</h3><p>反向引用表示引用前面分组匹配到的内容，\1 表示第一个，\2 表示第二个，以此类推</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">grep -E &#x27;([a-z]+)\1&#x27; file </span><br><span class="line"></span><br><span class="line">匹配至少一个的小写字母字符串并且这个字符串需要重复出现一次</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/linux/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/</id>
    <link href="https://threcial.github.io/linux/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/"/>
    <published>2026-05-20T02:41:19.000Z</published>
    <summary>Linux 中最常见的文本处理工具是：grep、sed、awk，也就是常说的“文本三剑客” grep：查找、过滤符合条件的行 sed：按行编辑、替换、删除、插入文本 awk：按列处理、条件判断、统计计</summary>
    <title>正则表达式</title>
    <updated>2026-05-20T02:41:19.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="文本处理" scheme="https://threcial.github.io/tags/%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86/"/>
    <category term="工具" scheme="https://threcial.github.io/tags/%E5%B7%A5%E5%85%B7/"/>
    <content>
      <![CDATA[<p>这三个工具作用分别是：sort 排序；uniq 去重和统计重复行；wc 统计行数、单词数、字节数</p><h2 id="sort"><a href="#sort" class="headerlink" title="sort"></a>sort</h2><p>sort能够对文本进行简单排序，默认是根据 ascii 码排序</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">-t    指定分隔符</span><br><span class="line">-k    指定排序使用的列，默认使用第一列</span><br><span class="line">-r    倒序</span><br><span class="line">-n    根据数值大小而非 ascii </span><br><span class="line">-o    将输出写入文件</span><br><span class="line">-u    去重，等效于 sort file | uniq</span><br></pre></td></tr></table></figure><h2 id="uniq"><a href="#uniq" class="headerlink" title="uniq"></a>uniq</h2><p>uniq 用来处理重复行，它有一个非常重要的特点：只能处理相邻的重复行，因此 uniq 要和 sort 一起使用，否则无法达到去重的效果</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">-c    在行首加上重复的次数</span><br><span class="line">-d    仅显示重复的行</span><br><span class="line">-u    仅显示不重复的行</span><br></pre></td></tr></table></figure><h2 id="wc"><a href="#wc" class="headerlink" title="wc"></a>wc</h2><p>wc 是 word count 的缩写，用来统计文本数量</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">常规输出为</span><br><span class="line">行数  单词数  字节数</span><br><span class="line">-l 只输出行数</span><br><span class="line">-w 只输出单词数</span><br><span class="line">-m 只统计字符数，换行符不计入统计，EOF会计入统计</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/linux/%E7%AE%80%E5%8D%95%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86-sort-uniq-wc/</id>
    <link href="https://threcial.github.io/linux/%E7%AE%80%E5%8D%95%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86-sort-uniq-wc/"/>
    <published>2026-05-20T01:28:55.000Z</published>
    <summary>这三个工具作用分别是：sort 排序；uniq 去重和统计重复行；wc 统计行数、单词数、字节数 sort sort能够对文本进行简单排序，默认是根据 ascii 码排序 -t 指定分隔符 -k 指定</summary>
    <title>简单文本处理-sort uniq wc</title>
    <updated>2026-05-20T01:28:55.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="rsync" scheme="https://threcial.github.io/tags/rsync/"/>
    <category term="远程同步" scheme="https://threcial.github.io/tags/%E8%BF%9C%E7%A8%8B%E5%90%8C%E6%AD%A5/"/>
    <content>
      <![CDATA[<p>rsync ，Linux 文件同步与增量备份工具，一般用于两台服务器远程同步文件</p><p>rsync具有以下特点：</p><ul><li>只同步发生变化的文件</li><li>支持本地同步</li><li>支持远程同步</li><li>支持保留权限、属主、时间戳、软链接</li><li>支持排除文件</li><li>支持删除目标端多余文件</li><li>支持断点续传和传输压缩</li></ul><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">安装</span><br><span class="line">yum install rsync -y</span><br><span class="line">远程同步时，本机和远程主机都需要安装 rsync</span><br><span class="line"></span><br><span class="line">基本用法</span><br><span class="line">rsync [选项] 源路径 目标路径</span><br><span class="line"></span><br><span class="line">rsync -av /data/ /backup/data/                         本地同步</span><br><span class="line">rsync -av /data/ root@192.168.174.127:/backup/data/    远程同步</span><br><span class="line">rsync -av root@192.168.174.127:/backup/data/ /data/    远程拉取</span><br><span class="line">注意：/data/ 等同 /data/* ，不包含 /data 本身这个目录，/data 则包含 /data 本身</span><br></pre></td></tr></table></figure><p>常见参数</p><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>-a</code></td><td>归档模式，保留权限、属主、时间戳、软链接等</td></tr><tr><td><code>-v</code></td><td>显示详细过程</td></tr><tr><td><code>-z</code></td><td>传输时压缩，适合远程同步</td></tr><tr><td><code>-P</code></td><td>显示进度，并支持断点续传</td></tr><tr><td><code>--delete</code></td><td>删除目标端多余文件，使目标和源完全一致</td></tr><tr><td><code>--exclude</code></td><td>排除指定文件或目录</td></tr><tr><td><code>--exclude-from</code></td><td>从文件读取排除规则</td></tr><tr><td><code>-n</code> &#x2F; <code>--dry-run</code></td><td>预演，不真正执行</td></tr><tr><td><code>--bwlimit</code></td><td>限制传输速度</td></tr><tr><td><code>-e</code></td><td>指定远程 shell，常用于指定 SSH 端口或私钥</td></tr></tbody></table><p>由于 rsync 在传输多个小型文件时容易出现进程僵死的问题，推荐用 tar 打包后传输</p><p>在使用 –delete 参数时，先使用 -n 参数预演，否则容易出现误删文件的情况</p><p>rsync 是基于 ssh 的，同时默认使用 22 端口，如果需要使用其他端口、指定私钥等操作，需要用到 -e 参数，.ssh&#x2F;config 文件也是生效的</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">rsync -av /data/ -e &quot;ssh -p 22222&quot; root@192.168.174.127:/backup/data/</span><br><span class="line">                          修改了ssh端口的情况</span><br><span class="line"></span><br><span class="line">假设在家目录下的 .ssh/config 中配置</span><br><span class="line">Host Clone</span><br><span class="line">Hostname 192.168.174.127</span><br><span class="line">Port 22022</span><br><span class="line">User root</span><br><span class="line">IdentityFile /root/.ssh/id_rsa</span><br><span class="line">那么可以使用如下命令便捷推送</span><br><span class="line">rsync -av /data/ Clone:/backup/data/</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/linux/rsync/</id>
    <link href="https://threcial.github.io/linux/rsync/"/>
    <published>2026-05-18T09:38:56.000Z</published>
    <summary>rsync ，Linux 文件同步与增量备份工具，一般用于两台服务器远程同步文件 rsync具有以下特点： 只同步发生变化的文件 支持本地同步 支持远程同步 支持保留权限、属主、时间戳、软链接 支持排</summary>
    <title>rsync</title>
    <updated>2026-05-18T09:38:56.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="linux" scheme="https://threcial.github.io/tags/linux/"/>
    <category term="网络" scheme="https://threcial.github.io/tags/%E7%BD%91%E7%BB%9C/"/>
    <content>
      <![CDATA[<h2 id="网络配置"><a href="#网络配置" class="headerlink" title="网络配置"></a>网络配置</h2><p>可通过 ip 和 ifconfig 来初步查看本机网络配置</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">yum install net-tools -y      ifconfig 安装</span><br><span class="line">ifconfig属于旧命令 推荐使用ip</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">ip addr                                          IP 地址</span><br><span class="line">ip addr show dev ens33                           网卡ens33的 IP</span><br><span class="line">ip addr add 192.168.174.128/24 dev ens33         给网卡添加 IP</span><br><span class="line">ip addr del 192.168.174.128/24 dev ens33         给网卡删除 IP</span><br><span class="line"></span><br><span class="line">ip link                                          网卡状态</span><br><span class="line">ip link show dev ens33                           只看ens33</span><br><span class="line">ip link set dev ens33 up                         </span><br><span class="line">ip link set dev ens33 down                       启用和关闭网卡</span><br><span class="line"></span><br><span class="line">ip route                                         路由表</span><br><span class="line">ip route add default via 192.168.174.2 dev ens33           添加默认路由</span><br><span class="line">ip route add 10.10.10.0/24 via 192.168.174.1 dev ens33     添加静态路由</span><br><span class="line">ip route del 10.10.10.0/24                                  删除</span><br><span class="line">ip route get 8.8.8.8                                   查看去往8.8.8.8使用的路由</span><br><span class="line"></span><br><span class="line">ip neigh                                          ARP 邻居表</span><br></pre></td></tr></table></figure><p>以上都是临时配置，永久配置需要修改网络配置文件，位于 &#x2F;etc&#x2F;sysconfig&#x2F;network-scripts&#x2F;</p><p>其中 route-ens33 可以配置路由，ifcfg-ens33 可以配置 ip</p><h2 id="网络状态"><a href="#网络状态" class="headerlink" title="网络状态"></a>网络状态</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">ss   用于查看当前端口状况</span><br><span class="line">-l  监听端口</span><br><span class="line">-a  全部端口</span><br><span class="line">-t  tcp</span><br><span class="line">-u  udp</span><br><span class="line">-n  不显示别名</span><br><span class="line">-p  显示进程名</span><br><span class="line">-i  网络接口</span><br></pre></td></tr></table></figure><h2 id="网络测试"><a href="#网络测试" class="headerlink" title="网络测试"></a>网络测试</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ping                    使用icmp包测试网络连通性 </span><br><span class="line">-c 指定包个数 -s指定大小    </span><br><span class="line">nslookup                域名解析查询</span><br><span class="line">traceroute              查看数据包经过的路由器，现已被大部分路由器屏蔽</span><br><span class="line">telnet        简单测试端口是否开放</span><br></pre></td></tr></table></figure><h2 id="下载"><a href="#下载" class="headerlink" title="下载"></a>下载</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">常用命令为 curl 和 wget</span><br><span class="line">curl 主要用于测试，wget 主要用于下载</span><br><span class="line"></span><br><span class="line">wget</span><br><span class="line">    -O 指定下载文件名 </span><br><span class="line">    -c 断点重传 </span><br><span class="line">    --limit-rate=10k 限速</span><br><span class="line"></span><br><span class="line">curl </span><br><span class="line">    -I 只显示回应数据包头 </span><br><span class="line">    -o 指定下载文件名</span><br><span class="line">    -O 下载，文件名同访问名</span><br><span class="line">    -L 跟随跳转 </span><br><span class="line">    -v 过程</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/linux/linux-%E7%BD%91%E7%BB%9C/</id>
    <link href="https://threcial.github.io/linux/linux-%E7%BD%91%E7%BB%9C/"/>
    <published>2026-05-17T11:06:49.000Z</published>
    <summary>网络配置 可通过 ip 和 ifconfig 来初步查看本机网络配置 yum install net-tools -y ifconfig 安装 ifconfig属于旧命令 推荐使用ip ip addr</summary>
    <title>linux-网络</title>
    <updated>2026-05-18T11:06:49.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="服务" scheme="https://threcial.github.io/tags/%E6%9C%8D%E5%8A%A1/"/>
    <category term="进程" scheme="https://threcial.github.io/tags/%E8%BF%9B%E7%A8%8B/"/>
    <content>
      <![CDATA[<h2 id="服务"><a href="#服务" class="headerlink" title="服务"></a>服务</h2><p>在 Linux 中，服务通常指一种长期运行在后台的程序，它启动后不会马上退出，而是持续等待用户、系统或其他程序调用</p><p>早期 Linux 使用 SysV init 来管理服务。CentOS 7 开始，systemd 取代了传统 init，成为系统初始化和服务管理的核心，并且 systemctl 兼容了部分传统 service 用法</p><p>systemd 管理的对象称为 unit</p><p>常见 unit 类型：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">.service   服务</span><br><span class="line">.target    启动目标</span><br><span class="line">.timer     定时器</span><br><span class="line">.socket    socket 激活</span><br><span class="line">.mount     挂载点</span><br><span class="line">.path      路径监听</span><br></pre></td></tr></table></figure><p>服务最常见的是 <code>.service</code> 文件。</p><p>systemd 相关目录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">/usr/lib/systemd/system/   软件包安装的 unit 文件</span><br><span class="line">/run/systemd/system/       运行时生成的 unit 文件</span><br><span class="line">/etc/systemd/system/       管理员自定义或覆盖的 unit 文件</span><br><span class="line">/etc/sysconfig/            某些服务的默认参数配置</span><br></pre></td></tr></table></figure><p><code>/etc/systemd/system/</code> 的优先级通常高于 <code>/usr/lib/systemd/system/</code>，适合管理员自定义覆盖服务配置</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">sytemctl 命令用于管理这些服务</span><br><span class="line"></span><br><span class="line">systemctl start nginx           启动nginx</span><br><span class="line">systemctl stop nginx            停止nginx</span><br><span class="line">systemctl restart nginx         重启nginx</span><br><span class="line">systemctl reload nginx          重载nginx</span><br><span class="line">systemctl status nginx          查看nginx状态</span><br><span class="line">systemctl enable nginx          设置开机自启</span><br><span class="line">systemctl disable nginx         关闭开机自启</span><br><span class="line">systemctl is-active nginx       检查服务是否已经启动</span><br><span class="line">systemctl is-enabled nginx查看服务是否开机自启动</span><br></pre></td></tr></table></figure><h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><p>程序是存放在磁盘上的静态文件，进程是程序运行后的实例。</p><p>每个进程都有一个唯一编号，称为 PID，同时进程之间有父子关系，一个进程可以创建子进程，子进程的 PPID 就是父进程的 PID</p><h3 id="0号进程、1号进程、2号进程"><a href="#0号进程、1号进程、2号进程" class="headerlink" title="0号进程、1号进程、2号进程"></a>0号进程、1号进程、2号进程</h3><p><strong>PID 0：swapper &#x2F; idle 进程</strong></p><p>PID 0 通常被称为：swapper &#x2F; idle process ，它是 Linux 内核启动早期创建的特殊进程，不是普通用户进程</p><p>它的主要作用是：<strong>当 CPU 没有其他可运行任务时，执行 idle 空闲循环</strong></p><p>也就是说，当系统中没有进程需要占用 CPU 时，CPU 并不是“停止工作”，而是进入空闲状态，这个空闲状态就和 PID 0 有关</p><p>PID 0 不会像普通进程一样出现在 ps 输出中，它属于内核调度体系的一部分，不能被 kill 也不是 systemd 管理的服务</p><p><strong>PID 1：init &#x2F; systemd</strong></p><p>PID 1 是第一个用户空间进程，在 CentOS 7 及之后的系统中，PID 1 为 systemd</p><p>PID 1 的地位非常特殊，它负责系统初始化、启动系统服务、管理服务依赖、回收孤儿进程、维护系统运行状态等</p><p>如果一个父进程退出了，但它的子进程还在运行，这个子进程就会变成孤儿进程，孤儿进程会由 PID 1 收养</p><p><strong>PID 2：kthreadd</strong></p><p>kthreadd 主要负责创建和管理其他内核线程</p><h3 id="静态查看进程"><a href="#静态查看进程" class="headerlink" title="静态查看进程"></a>静态查看进程</h3><p>ps 静态进程管理命令，常用参数 -ef 和 aux</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">-e 全部进程</span><br><span class="line">-f 完整显示</span><br><span class="line">-l 长格式显示</span><br><span class="line">-p 指定pid</span><br><span class="line">-u 指定用户</span><br><span class="line">-H 树状显示</span><br></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>USER</code></td><td>进程所属用户</td></tr><tr><td><code>PID</code></td><td>进程号</td></tr><tr><td><code>%CPU</code></td><td>CPU 占用百分比</td></tr><tr><td><code>%MEM</code></td><td>内存占用百分比</td></tr><tr><td><code>VSZ</code></td><td>虚拟内存大小</td></tr><tr><td><code>RSS</code></td><td>实际物理内存占用</td></tr><tr><td><code>TTY</code></td><td>启动该进程的终端</td></tr><tr><td><code>STAT</code></td><td>进程状态</td></tr><tr><td><code>START</code></td><td>进程启动时间</td></tr><tr><td><code>TIME</code></td><td>累计 CPU 时间</td></tr><tr><td><code>COMMAND</code></td><td>启动命令</td></tr></tbody></table><p>常见 stat</p><ul><li><strong>R running</strong> 运行中或可运行</li><li><strong>S sleeping</strong> 可中断睡眠</li><li><strong>D uninterruptible sleep</strong> 不可中断睡眠，常见于 I&#x2F;O 等待</li><li><strong>T stopped</strong> 暂停</li><li><strong>Z zombie</strong> 僵尸进程</li><li><strong>s session leader</strong> 会话首进程</li><li><strong>+</strong> 前台进程组</li><li><strong>&lt;</strong> 高优先级</li><li><strong>N</strong> 低优先级</li></ul><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">pstree 树状显示进程</span><br><span class="line"></span><br><span class="line">安装 yum install psmisc -y</span><br><span class="line">使用 pstree -p 与pid一起显示</span><br></pre></td></tr></table></figure><h3 id="动态查看进程"><a href="#动态查看进程" class="headerlink" title="动态查看进程"></a>动态查看进程</h3><p>top 实时显示进程情况，用法 <a href="https://threcial.cn/linux/linux-%e7%b3%bb%e7%bb%9f%e4%bf%a1%e6%81%af%e6%9f%a5%e7%9c%8b%e4%b8%8e%e6%80%a7%e8%83%bd%e6%8e%92%e6%9f%a5%e5%9f%ba%e7%a1%80/">Linux 系统信息查看与性能排查基础</a></p><h3 id="进程管理"><a href="#进程管理" class="headerlink" title="进程管理"></a>进程管理</h3><p><strong>pidof 和 pgrep 用于根据进程名查找 pid</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">pidof sshd  查看sshd的pid</span><br><span class="line"></span><br><span class="line">pgrep比pidof更加灵活，类似grep不需要完全匹配而是部分匹配</span><br><span class="line"></span><br><span class="line">pgrep ssh 也能查看到sshd的pid</span><br><span class="line">pgrep -P ppid 根据父进程pid查找子进程pid</span><br><span class="line">pgrep -u username 根据用户查找</span><br></pre></td></tr></table></figure><p><strong>kill用于给进程发送信号</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">kill -l 显示可用信号</span><br><span class="line"></span><br><span class="line">常见</span><br><span class="line">-15 终止</span><br><span class="line">-9 强制终止</span><br><span class="line">-19 暂停</span><br><span class="line">-18 继续运行</span><br><span class="line"></span><br><span class="line">kill -19 1234 暂停1234进程的运行</span><br><span class="line">killall ping   终止所有ping进程</span><br></pre></td></tr></table></figure><p><strong>后台任务</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">通过 &amp; 可以让一个命令在后台执行，如：</span><br><span class="line">ping www.qq.com &gt;&gt; ping.log 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure><p>jobs 查看当前 shell 的后台任务，注意只能查看当前 shell 的后台任务，除此之外的进程都看不了</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">fg %1        切换到前台运行</span><br><span class="line">bg %1        切换到后台运行</span><br><span class="line">             其中 %1 是 jobs 中显示的任务编号</span><br></pre></td></tr></table></figure><p>&amp; 只是让程序在后台运行，不一定能保证断开终端后继续运行，如果需要断开终端后继续运行，需要用 nohup</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">nohup ping www.qq.com &gt;&gt; ping.log 2&gt;&amp;1 &amp;</span><br><span class="line"></span><br><span class="line">nohup 的作用是让进程忽略 SIGHUP 信号，nohup 本身不负责后台运行，后台运行是 &amp; 的作用</span><br><span class="line"></span><br><span class="line">注:通过kill -l 可以看到1号信号就是hup信号</span><br></pre></td></tr></table></figure><p><strong>lsof : list open files</strong></p><p>显示进程所打开的文件，也就是占用情况</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">lsof -p 1234                1234进程打开的文件</span><br><span class="line">lsof -u root                root用户使用的文件</span><br><span class="line">lsof /var/log/messages      messages文件被哪些进程使用</span><br><span class="line">lsof -i:22                  22端口的占用</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/linux/linux%E6%9C%8D%E5%8A%A1%E4%B8%8E%E8%BF%9B%E7%A8%8B/</id>
    <link href="https://threcial.github.io/linux/linux%E6%9C%8D%E5%8A%A1%E4%B8%8E%E8%BF%9B%E7%A8%8B/"/>
    <published>2026-05-17T07:41:38.000Z</published>
    <summary>服务 在 Linux 中，服务通常指一种长期运行在后台的程序，它启动后不会马上退出，而是持续等待用户、系统或其他程序调用 早期 Linux 使用 SysV init 来管理服务。CentOS 7 开始</summary>
    <title>linux服务与进程</title>
    <updated>2026-05-17T07:41:38.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Threcial</name>
    </author>
    <category term="linux" scheme="https://threcial.github.io/categories/linux/"/>
    <category term="定时任务" scheme="https://threcial.github.io/tags/%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1/"/>
    <category term="打包" scheme="https://threcial.github.io/tags/%E6%89%93%E5%8C%85/"/>
    <category term="日志" scheme="https://threcial.github.io/tags/%E6%97%A5%E5%BF%97/"/>
    <category term="自动化运维" scheme="https://threcial.github.io/tags/%E8%87%AA%E5%8A%A8%E5%8C%96%E8%BF%90%E7%BB%B4/"/>
    <content>
      <![CDATA[<p>在 Linux 运维中，tar、crontab、logrotate 是三个非常基础但非常重要的工具。它们分别解决三个问题：tar 负责把文件打包、压缩、备份，crontab 负责定时执行任务，logrotate 负责自动切割日志，防止日志无限增长，这三者经常组合使用。例如：每天凌晨自动打包网站目录、定期备份配置文件、定期清理或切割日志</p><h2 id="tar"><a href="#tar" class="headerlink" title="tar"></a>tar</h2><p>不同于 windows 下的打包压缩工具，在 linux 中，需要把打包和压缩分开看，打包就是把多个文件或目录合并成一个文件，压缩则是通过算法减少文件体积</p><p>tar 本身主要负责打包，配合其他压缩算法才能做到打包压缩，比如常见的 .tar.gz 文件，就是经过 tar 打包后再用 gzip 进行压缩</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">tar -cvf data.tar ./data</span><br><span class="line"></span><br><span class="line">-c  create    创建归档包</span><br><span class="line">-v  verbose   显示打包过程</span><br><span class="line">-f  file      指定归档文件名</span><br><span class="line">./data        要打包的目录</span><br><span class="line"></span><br><span class="line">使用 -z 参数即可使用 gzip 进行压缩</span><br><span class="line">tar -zcvf data.tar.gz ./data</span><br><span class="line"></span><br><span class="line">tar -tf data.tar.gz  查看压缩包内容</span><br><span class="line">tar -zxf data.tar.gz -C /root/   指定解压目录</span><br></pre></td></tr></table></figure><h3 id="gzip"><a href="#gzip" class="headerlink" title="gzip"></a>gzip</h3><p>gzip 主要用于压缩单个文件，无法压缩目录</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">gzip file                 压缩文件，默认删除源文件</span><br><span class="line">gzip -r /root/data         递归压缩</span><br><span class="line">gzip -v file              压缩并显示过程</span><br><span class="line">gzip -c file &gt; file.gz    压缩并保留源文件</span><br><span class="line"></span><br><span class="line">gzip -d file.gz           解压，默认删除 .gz 文件</span><br><span class="line">gzip -dc file.gz &gt; file   解压，保留 .gz 文件</span><br><span class="line"></span><br><span class="line">gzip -t file.gz</span><br><span class="line">gzip -tv file.gz           测试压缩包</span><br><span class="line"></span><br><span class="line">gzip -1c file &gt; file-1.gz</span><br><span class="line">gzip -9c file &gt; file-9.gz   指定压缩级别，默认为6</span><br><span class="line"></span><br><span class="line">zless file.gz            查看压缩文件</span><br><span class="line"></span><br><span class="line">zgrep &#x27;keyword&#x27; file.gz</span><br><span class="line">zgrep -n &#x27;keyword&#x27; file.gz    搜索压缩文件内容，-n显示行号</span><br></pre></td></tr></table></figure><h3 id="排除文件、指定文件"><a href="#排除文件、指定文件" class="headerlink" title="排除文件、指定文件"></a>排除文件、指定文件</h3><p>备份时如果有不希望被打包压缩的文件，可以使用 –exclude</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">tar -zcf site.tar.gz --exclude=*.log ./site     排除.log结尾的文件</span><br><span class="line">tar -zcf backup.tar.gz --exclude-from=exclude.list ./site   根据文本文件内容排除</span><br></pre></td></tr></table></figure><p><strong>注意：打包目标用相对路径，exclude 也用相对路径，打包目标用绝对路径，exclude 也用绝对路径</strong></p><p>一个好的习惯可以解决很多意想不到的问题，详情可见 <a href="https://threcial.cn/linux/tar-exclude/">tar –exclude</a></p><p>有排除也有指定</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-T 从指定文件中获取文件名来解压或创建文件</span><br><span class="line">tar -zcf backup.tar.gz -T include.list  打包压缩 include.list 中的文件</span><br></pre></td></tr></table></figure><h3 id="与-find-联动"><a href="#与-find-联动" class="headerlink" title="与 find 联动"></a>与 find 联动</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">打包当前目录下大于 10KB 的文件</span><br><span class="line">find . -type f -size +10k | xargs tar -zcf bigfiles.tar.gz </span><br></pre></td></tr></table></figure><h2 id="crontab"><a href="#crontab" class="headerlink" title="crontab"></a>crontab</h2><p>定时任务三个概念：</p><ul><li><strong>cron</strong> 定时任务体系</li><li><strong>crond</strong> 定时任务服务进程</li><li><strong>crontab</strong> 管理定时任务的命令</li></ul><p>作为一个服务，可以使用 systemctl 来查看，crontab 用于配置</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">crontab -e                       编辑定时任务</span><br><span class="line">crontab -l                       查看定时任务</span><br><span class="line">crontab -u username -l           查看指定用户的定时任务</span><br><span class="line"></span><br><span class="line">在 /var/spool/cron 中存放着每个用户的定时任务文件，命名为用户名</span><br></pre></td></tr></table></figure><h3 id="cron-格式"><a href="#cron-格式" class="headerlink" title="cron 格式"></a>cron 格式</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">* * * * * command</span><br><span class="line"></span><br><span class="line">前五个*号分别表示：</span><br><span class="line">分    0-59</span><br><span class="line">时    0-23</span><br><span class="line">日    1-31</span><br><span class="line">月    1-12</span><br><span class="line">周    0-7，0 和 7 通常都表示周日</span><br><span class="line"></span><br><span class="line">特殊表示方式</span><br><span class="line">*      任意时间</span><br><span class="line">-      时间范围</span><br><span class="line">,      多个指定时间</span><br><span class="line">*/n    每隔 n 个单位</span><br><span class="line"></span><br><span class="line">如每天 8 点到 15 点整点执行command：</span><br><span class="line">0 8-15 * * * command</span><br></pre></td></tr></table></figure><p>cron 由于环境变量问题，不推荐直接在定时任务文件里执行命令，而是写好脚本，定时执行脚本</p><p>同时这些输出应该被重定向到其他文件中</p><h2 id="logrotate"><a href="#logrotate" class="headerlink" title="logrotate"></a>logrotate</h2><p>无论是备份还是日志，都会有一个问题，服务器运行时间越长，日志文件就越大，针对这个问题，就需要用到 logrotate 日志切割</p><p>&#x2F;etc&#x2F;logrotate.conf 为 logrotate 主配置文件，&#x2F;etc&#x2F;logrotate.d&#x2F; 是子配置目录，推荐将配置放入子配置目录中，便于管理</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">配置内容格式为：</span><br><span class="line">日志文件路径 &#123;</span><br><span class="line">    配置项</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">例如</span><br><span class="line">/var/log/myapp/*.log &#123;</span><br><span class="line">    daily                        每日检查，weekly每周、monthly每月</span><br><span class="line">    rotate 7                      保留七份日志 </span><br><span class="line">    size  5k                      切割大小为5k</span><br><span class="line">    compress                      压缩</span><br><span class="line">    dateext                       以日期时间命名日志切割文件</span><br><span class="line">    create 0644 root root          创建的新日志权限与所属</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>系统通过系统级每日任务来调用 logrotate，<em>cat &#x2F;etc&#x2F;cron.daily&#x2F;logrotate</em> 可以查看</p><p>如果想要测试新写入的配置使用如下命令</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">logrotate -d /etc/logrotate.d/刚写入的配置文件   测试</span><br><span class="line">logrotate -f /etc/logrotate.d/刚写入的配置文件   强制切割，也就是立马执行一次</span><br><span class="line">logrotate -vf /etc/logrotate.d/刚写入的配置文件   -v 显示过程</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://threcial.github.io/linux/linux%E8%87%AA%E5%8A%A8%E5%8C%96%E8%BF%90%E7%BB%B4-tar-crontab-logrotate/</id>
    <link href="https://threcial.github.io/linux/linux%E8%87%AA%E5%8A%A8%E5%8C%96%E8%BF%90%E7%BB%B4-tar-crontab-logrotate/"/>
    <published>2026-05-17T03:58:57.000Z</published>
    <summary>在 Linux 运维中，tar、crontab、logrotate 是三个非常基础但非常重要的工具。它们分别解决三个问题：tar 负责把文件打包、压缩、备份，crontab 负责定时执行任务，logr</summary>
    <title>linux自动化运维-tar crontab logrotate</title>
    <updated>2026-05-17T03:58:57.000Z</updated>
  </entry>
</feed>
